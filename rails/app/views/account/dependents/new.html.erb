<section class="account-section">
  <header class="section-header">
    <div>
      <p class="eyebrow"><%= t("account.dependents.new_header.eyebrow") %></p>
      <h2><%= t("account.dependents.new_header.title") %></h2>
      <p><%= t("account.dependents.new_header.subtitle") %></p>
    </div>
  </header>

  <%= render "account/dependents/form" %>
</section>
