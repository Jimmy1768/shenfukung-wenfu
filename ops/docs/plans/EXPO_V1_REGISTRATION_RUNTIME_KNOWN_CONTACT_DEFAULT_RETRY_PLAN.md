# Expo V1 Registration Runtime Known Contact Default Retry Plan

Status: accepted for direct runtime dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base:
`638e3b0353118d826cab0f053167f4a672ef5e8f`

Accepted parent method:
`ops/docs/plans/EXPO_V1_REGISTRATION_RUNTIME_KEYBOARD_DISMISSAL_RETRY_PLAN.md`

Accepted precondition-gap report:
`ops/docs/handoffs/2026-08-12-expo-v1-registration-runtime-keyboard-dismissal-retry-control-b.md`

## Source-Reconciled Default

The accepted attempt proved keyboard-aware `Runtime Dependent` creation, then
stopped because the self-service preparation contact field was nonempty while
the plan required it to be empty. Local source confirms the nonempty field is
intentional and authoritative:

- `mobile/app/dummy/repository.js` `newRegistration` sets `contact_name` to
  `state.profile.name`;
- `mobile/app/account/registration_authority.js` likewise defaults
  `contact_name` from `snapshot.profile.name`; and
- canonical dummy fixture profile name is exactly `林小安`.

This is a Planning precondition correction, not product-source repair. The
contact field must be exactly `林小安` after canonical reset and self-service
preparation. Any other value is a stop.

## Objective And Inherited Authority

Complete the same remaining registration evidence through Control-owned input:

1. create exactly one `Runtime Dependent`;
2. create one self `線上祈福服務` draft at the accepted default contact and
   quantity `1`;
3. edit/reopen it at quantity `2` and `Runtime Contact`, retaining immutable
   `TWD 600` and no duplicate;
4. create/reopen one Runtime Dependent `平安祈福` draft, retaining immutable
   `TWD 1,200` and no duplicate; and
5. retain paid-read-only, reset, and cleanup evidence.

Every parent-plan serial/package/foreground, local Metro/no-QR, test/lint/
verify, dependency, fresh UI-bound, keyboard lifecycle, stop-on-uncertainty,
reset, cleanup, terminal, and exclusion rule remains authoritative unless this
plan explicitly replaces the empty-contact precondition below.

Control creates one fresh isolated codex-prefixed packet. At most one ephemeral
Implementer may prepare/static-review the report only. Control owns all
target-fenced runtime/device/input/save/reset/cleanup/integration actions. No
Director callback or manual input is permitted.

## Authorized Text Editing

Permitted inserted ASCII values remain only:

```text
Runtime Dependent
2
Runtime Contact
```

For an existing exact field value, Control may additionally send
`KEYCODE_MOVE_END` once while that expected field is visibly focused, followed
by the exact deletion count named below. After each deletion sequence, it must
verify the exact expected rendered result before inserting anything. No select
all, long press, clipboard, IME action, approximate deletion, or correction is
allowed.

After each inserted text value, apply the parent keyboard lifecycle exactly:
prove IME present, send one Back, prove IME absent with the same form/package
and value retained, capture fresh post-IME evidence, and newly resolve any
Create/Update control. Never reuse pre-IME coordinates.

## Step 1 — Runtime Dependent

From canonical reset, reproduce the accepted passing path:

1. prove Runtime Dependent absent;
2. open Add Dependent with empty name and accepted prefilled relationship;
3. enter exact `Runtime Dependent`;
4. dismiss/prove IME lifecycle;
5. freshly resolve and tap Create once; and
6. prove exactly one Runtime Dependent and no duplicate.

## Step 2 — Create Self Service Draft With Canonical Default

1. From Discover select `線上祈福服務`.
2. Require immutable `TWD 600`, self selected, quantity `1`, and contact name
   exactly `林小安`.
3. Do not edit the canonical contact default during initial creation.
4. With IME absent, freshly resolve and tap Create exactly once.
5. Prove exactly one self service draft, no duplicate, and paid fixture
   retained.

If contact is empty or differs from exact `林小安`, stop without Create.

## Step 3 — Edit Self Service Draft

Reopen that draft and require offering `線上祈福服務`, `TWD 600`, self,
quantity exactly `1`, and contact exactly `林小安`.

### Quantity

1. Freshly focus quantity and prove IME visible plus rendered value `1`.
2. Send `KEYCODE_MOVE_END` once, then `KEYCODE_DEL` exactly once.
3. Require the quantity field empty.
4. Enter exact `2` once.
5. Apply the complete one-Back keyboard lifecycle and prove `2` retained on
   the same edit form.

### Contact name

1. Freshly focus contact name and prove IME visible plus exact `林小安`.
2. Send `KEYCODE_MOVE_END` once, then `KEYCODE_DEL` exactly three times—one
   for each known Unicode character.
3. Require the contact field exactly empty. If any character remains or any
   other field changes, stop without correction or Update.
4. Enter exact ASCII `Runtime Contact` once.
5. Apply the complete one-Back keyboard lifecycle and prove the exact value
   retained on the same edit form.

After both fields pass, require quantity `2`, contact `Runtime Contact`, and
unchanged offering/fee/self. Capture fresh post-IME evidence, newly resolve
Update, tap it once, prove one draft/no duplicate/paid fixture retained, then
reopen and prove all saved values and immutable authority.

## Step 4 — Dependent Blessing Draft

Execute the accepted parent path unchanged:

1. prepare `平安祈福`, immutable `TWD 1,200`, quantity `1`, and the expected
   registrant choices with no existing Runtime Dependent pair;
2. require IME absent;
3. freshly resolve and tap Runtime Dependent once;
4. verify it selected and self not selected;
5. freshly resolve and tap Create once;
6. prove exactly one dependent draft/no duplicate, plus edited service draft
   and paid fixture retained; and
7. reopen and prove dependent plus immutable offering/fee.

## Final Guard, Reset, And Cleanup

Reconfirm paid display-only/no checkout; use visible dummy reset once; prove
removal of Runtime Dependent and both drafts plus restoration of canonical
fixture dependent, paid registration, and catalog; stop only packet Metro;
remove exact serial reverse, temporary dependency symlink, and packet UI
evidence; prove no listener/reverse/symlink/evidence/Git/staging residue while
preserving installed client and camera permission.

On early stop, reset only from a visibly safe resettable TempleMate state.
Never clear package storage or blindly retry.

## Evidence And Terminal

The immutable report records:

- intentional canonical `林小安` default at preparation and reopen;
- exact known-value deletion counts and empty-result proof;
- every parent IME-present/one-Back/IME-absent/same-form/value-retained proof;
- fresh post-IME button/choice bounds and single actions;
- dependent, both saved/reopened drafts, paid fixture, reset, and cleanup;
- no Director input/request; and
- first prevented criterion with product versus evidence-method boundary.

Completion classification:
`expo_v1_registration_authority_runtime_validation_complete`.

Other accepted classifications remain:

- `expo_v1_registration_authority_runtime_defect_found`;
- `expo_v1_registration_authority_runtime_evidence_partial`;
- `runtime_cleanup_reconciliation_required`;
- `no_evidence_backed_direct_repair_remaining`.

## Explicit Exclusions

- no Director/manual callback or input;
- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no deletion unless the exact expected current value is proved; no extra
  delete, select-all, long press, stale coordinates, IME action, Enter/Tab/
  suggestion, keyboard-hide command, second/global Back, clipboard, alternate
  IME, accessibility, inspector, instrumentation, storage/package mutation,
  reinstall, or system-UI workaround;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no build/EAS/APK/AAB/signing/store/OTA/version/build change; retain
  `1.0.0 / Android 1 / iOS 1`;
- no SSH/production checkout/Candidate B/preflight/cleanup/ref/migration/
  restart/flag/provider-validation/remediation action.

Current classification:
`expo_v1_registration_runtime_known_contact_default_retry_authorized`.

First blocker: none at dispatch. Control owns bounded input and must not request
Director action.
