# TempleMate Development-Client Cash-Only Demo Parity Device Continuation Plan

Status: accepted for direct continuation dispatch to Control B after commit

Accepted: 2026-08-13

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical continuation base: `2440f72c289d2c2dcc4638845ddd1ea892571c6b`

Parent plan:
`ops/docs/plans/TEMPLEMATE_DEV_CLIENT_CASH_ONLY_DEMO_PARITY_PLAN.md`

Stopped terminal record:
`f434883543a594070a3fcd91ebe64bc409182cb6`

Preserved candidate worktree:
`/private/tmp/shengfukung-wenfu-templemate-cash-only-demo-parity`

Preserved branch: `codex/templemate-cash-only-demo-parity`

## Changed Prerequisite Evidence

Planning directly observed the exact target after the stopped packet:

- ADB serial `39011FDJH00FQ8` is in `device` state over USB;
- model is Pixel 8 / `shiba`; and
- installed package `com.jimmy1768.komainu.dev` has a readable base APK path.

Canonical `main` is clean/staging empty at the exact parent base. The preserved
candidate remains fenced and unstaged with exactly the eight parent-owned Expo
paths reported by Control B. No candidate source has been merged.

## Objective

Resume only the physical-development-client evidence omitted when the Pixel was
unavailable. Reverify the preserved candidate, attach the existing installed
development client using the accepted USB/local-Metro method, exercise the
parent Phase 2 cash-only demo journey, clean up packet-owned runtime state, and
accept/commit/integrate the preserved source only if the complete parent
criteria pass.

## Authorized Sequence

1. Verify exact parent/candidate ancestry, owned dirty paths, empty staging,
   canonical clean state, source equivalence, and absence of unowned residue.
2. Re-run full mobile tests, lint, verify, and offline Expo Doctor using the
   accepted byte-identical temporary dependency-tree method; remove the link
   after validation.
3. Reverify exact Pixel serial, package, launcher, version `1.0.0`, Android code
   `1`, target SDK 36, and clean local port/reverse preconditions.
4. Use only the previously accepted method: exact target-fenced
   `adb reverse tcp:8081 tcp:8081`, local dummy/development Metro, and local
   TempleMate dev-client attachment. Do not use a Metro QR, Expo launcher
   scanner, Pixel native scanner, or Director manual input.
5. Using bounded Control-owned device input, prove:
   - exactly four Shengfukung offerings and `NT$50` each;
   - no ambiguous fifth/inferred offering;
   - one self and one owned-dependent registration journey;
   - newly created registration visibly pending with online unavailable/cash
     arrangement guidance and no payment/provider/admin action;
   - completed-cash fixture visibly demo-only/read-only;
   - deterministic reset restores the canonical fixture state without
     duplicate or residue.
6. Stop on the first reproducible functional defect. A JavaScript-only defect
   within unchanged parent criteria is a Control-owned bounded repair; no
   Planning traffic is needed before the terminal outcome. Native/dependency,
   product-semantic, or authority changes are true Planning gaps.
7. Stop Metro, remove only the exact reverse/temp link/evidence, prove no
   listener/reverse/residue, and retain the installed development client.
8. If every parent criterion passes, stage and commit only the eight accepted
   source/test paths plus Control records, integrate into clean canonical main,
   and return one immutable terminal packet. Otherwise preserve the candidate
   and return the truthful terminal classification.

## Immutable Acceptance Criteria

- All parent source checks remain green with no dependency/config/version/
  native change.
- Exact physical evidence proves the six Phase 2 behaviors listed above.
- No checkout, fake/ECPay claim, payment mutation, admin surface, ambiguous
  offering, duplicate, or reset residue is observed.
- Cleanup and exact changed-path review pass.
- Accepted source is committed and canonically integrated only after complete
  physical evidence; canonical and isolated worktrees end clean/staging empty.
- No excluded action occurs.

## Explicit Exclusions

No holistic UI refinement, Director screen review, Rails/Vue/deployment/live
API/OAuth/provider, QR scan, dependency/native rebuild/install/EAS/artifact,
version/build increment, TestFlight/AAB/store/release/push, production data,
secret, payment, or external action.

## Terminal Boundary

Phase 2 completes only on affirmative evidence for all parent criteria. After
accepted integration, Planning releases Control B and stops at Phase 3. The
next gap is Director-owned holistic screen/state, layout, style, interaction,
accessibility, and UI analysis; no Phase 3 implementation is implied.

Current blocker: none. The exact Pixel/package prerequisite is directly
observed available.
