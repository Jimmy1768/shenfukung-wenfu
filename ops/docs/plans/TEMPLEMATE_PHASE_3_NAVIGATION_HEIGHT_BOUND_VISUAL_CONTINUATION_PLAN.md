# TempleMate Phase 3 Navigation Height Bound Visual Continuation Plan

Status: accepted for direct runtime dispatch after commit

Accepted: 2026-08-16

Owner: Wenfu Planning / Director

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Accepted base: canonical `main`
`69b9865f84a5bd8a4e1c32e699051e1c1bc65c91`

Accepted repaired source:
`90ebd1461fe16644c54e85b73c29719585b89f93`

Predecessor report:
`ops/docs/handoffs/2026-08-16-templemate-phase3-navigation-height-runtime-confirmation-control-b.md`

## Objective

Load the accepted repaired bundle, have Control establish the public dummy
account session without Director text input, pause for TempleMate's required
physical trusted-demo-temple QR scan, then hold the bound shell for the
Director's visual confirmation of the compact navigation height.

No source repair is authorized.

## Entry And Preparation

- Exact serial `39011FDJH00FQ8`, Pixel 8 / shiba.
- Exact package `com.jimmy1768.komainu.dev`, version `1.0.0`, code `1`, target
  SDK 36, existing MainActivity.
- Exact clean source containing `90ebd14`; exact package/lockfile identity;
  clear port 8081/reverse; device awake/unlocked; prior stay-awake recorded.
- At most one project-local `yarn install --frozen-lockfile`, registry only for
  missing locked archives; no manifest/lockfile/version/config/global change.
- Focused navigation tests, full mobile suite, lint, and verify must pass.
- Use temporary USB-only stay-awake `2`, restored exactly at closeout.

## Ordered Attachment

Once only:

1. force-stop the exact package without clearing data/cache;
2. start exact MainActivity without URL and verify DevLauncher ready;
3. start one dummy localhost Metro 8081 and verify ready;
4. create the exact serial `tcp:8081` reverse; and
5. deliver the established local `exp+templemate` URL once.

No Metro QR/scanner, Pixel native scanner, or alternate attachment.

## Control-Owned Dummy Session Preparation

After a stable signed-out dummy surface appears, Control may use fresh
package-scoped UI hierarchy evidence and ordinary target-fenced input to:

- enter the public fixture email `member@example.test`;
- enter the public fixture password `templemate-demo`;
- dismiss the keyboard when required using one ordinary Back;
- resolve the fresh visible Sign in control after keyboard dismissal; and
- activate Sign in exactly once.

Control must verify the accepted authenticated-unbound QR-first gate. No
Director text input, clipboard, accessibility/keyboard setting change,
OAuth/provider action, alternate account, registration, reset, or other app
mutation is authorized.

## Trusted QR Callback

When TempleMate's unbound gate is visibly ready, Control sends exactly:

`director_action_required: navigation_height_trusted_demo_temple_qr`

Planning will present the existing trusted demo-temple QR image. The Director
uses only TempleMate's in-app `Scan demo QR` / CameraView to scan it. Control
must not inspect or retain QR pixels, payload, or media, and no Expo launcher
or native scanner may be used.

After the Director confirms the scan, Control verifies the bound temple shell.

## Bound Visual Confirmation

Control verifies:

- Header shows Settings then Sign out with readable app/temple identity;
- business navigation shows exactly five ordered destinations on one line;
- navigation occupies its natural compact single-row height;
- pills are vertically centered and retain readable labels, padding, and
  minimum touch height;
- active content begins immediately below the compact menu; and
- there is no wrapping, vertical stretching, overlap, Render Error, fatal
  overlay, JavaScript exception, or native exit.

Then send exactly:

`director_action_required: navigation_height_visual_confirmation`

Hold Metro, reverse, and temporary stay-awake state for the Director's one
confirmation. Ask for no additional navigation or input.

## Cleanup

After confirmation or terminal failure:

- stop packet Metro;
- remove exact reverse and verify listener/reverse absent;
- remove packet-created `mobile/node_modules` and sanitized evidence;
- restore/read back prior stay-awake value;
- preserve installed app and current app data; do not reset or sign out;
- verify manifests/lockfile and Git unchanged/clean; and
- return one immutable terminal packet directly to Planning.

## Explicit Exclusions

No source/test edit, non-fixture account, Director manual text input, app-data/
cache clear, reset/sign-out, reinstall/reboot, OAuth/provider/browser, real API,
payment, non-TempleMate QR/scanner, dependency/lockfile/config/native/version/
build change, rebuild/EAS, Rails/Vue, production/deployment/release/push,
secret, or external mutation.

Current blocker: none if the Pixel remains awake, unlocked, and accessible.
