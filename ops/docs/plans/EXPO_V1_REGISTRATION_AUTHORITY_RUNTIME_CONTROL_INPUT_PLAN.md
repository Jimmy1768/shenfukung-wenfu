# Expo V1 Registration Authority Runtime Control Input Plan

Status: accepted for direct continuation dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base:
`736004b1a47ac321e3d616faee5f561ac79b61ad`

Superseded input method:
`ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_DIRECTOR_SETUP_CALLBACK_PLAN.md`

Accepted source and evidence lineage:

- `693e6dcbaa8525e05fea2b133dbed956d86d6537` — registration-authority source;
- `72e8476899197f28f6ac428851d2613fe236a83d` — accepted partial runtime evidence;
- `5123eee9a58aa2da53ae9f5bd5355623fa4ce3b4` — accepted foreground/setup-gap report.

## Director Override And Changed Device Evidence

The Director rejects manual input for this runtime evidence run. Control B
must own all bounded form input and selection. No further
`director_action_required` callback is permitted for dependent creation, self
draft editing, dependent selection, foregrounding, or any other step.

Planning directly confirmed:

- exact serial `39011FDJH00FQ8` is connected as Pixel 8 / `shiba`;
- the new USB location is `usb:1-1.1`, transport ID `3`;
- canonical repository remains
  `/Users/jimmy1768/Projects/shengfukung-wenfu`;
- this Planning task's generated workspace is
  `/Users/jimmy1768/.codex/worktrees/a56a/shengfukung-wenfu`; and
- Control's read-only follow-up found
  `com.jimmy1768.komainu.dev/.MainActivity` still focused with the prepared Add
  Dependent form untouched.

The serial, package, and visible UI state are authoritative. USB bus location
and transport ID may change again and must not be used as the mutation target.

## Objective

Complete the same two remaining registration rows without Director input:

1. create exactly one temporary dependent named `Runtime Dependent` with the
   existing prefilled relationship;
2. create and then edit one self `線上祈福服務` draft to quantity `2` and
   contact name `Runtime Contact`, retaining immutable `TWD 600`;
3. create one `平安祈福` draft for `Runtime Dependent`, retaining immutable
   `TWD 1,200`; and
4. reconfirm the paid fixture, reset, and exact cleanup.

This changes only the evidence-input method. It does not change product source,
the registration authority contract, or accepted runtime criteria.

## Continuation And Entry Fence

Control may continue the currently preserved packet/runtime session if and
only if all of these read-only checks pass immediately before input:

- exact serial `39011FDJH00FQ8` is in state `device` and identifies Pixel 8 /
  `shiba`;
- focused package/activity is exactly
  `com.jimmy1768.komainu.dev/.MainActivity`;
- a fresh package-scoped UI hierarchy proves the expected Add Dependent form,
  empty name field, accepted prefilled relationship, and Create control;
- no text, selection, Create, registration mutation, reset, developer overlay,
  NotificationShade, package/storage mutation, or source change occurred after
  the supersession acknowledgement;
- packet Metro/listener and exact serial `tcp:8081` reverse are either both
  still the existing accepted session or both absent; never create a duplicate
  Metro/reverse session; and
- canonical and isolated repository states remain attributable, with any
  later canonical commits limited to accepted OAuth documentation and this
  committed replacement plan.

If the preserved form/session is no longer exact, Control may clean up that
packet and reproduce the parent plan's accepted foreground/USB Metro/reset
setup once. It must not ask the Director to foreground, unlock, dismiss system
UI, or enter/select anything. If deterministic reproduction cannot be done
with the already accepted Control-owned navigation method, stop and report the
first prevented criterion.

## Authorized Control Input Method

Control may use only target-fenced ADB against exact serial
`39011FDJH00FQ8`, plus fresh package-scoped UI hierarchy and screenshot
evidence, for these bounded actions:

- tap a visible field/control at coordinates resolved from a fresh current UI
  node or visible screenshot;
- enter ASCII through `adb shell input text` into a visibly focused, expected
  field;
- send only the minimum navigation/edit key events needed for a field whose
  current value was visibly verified; and
- tap one visible registrant choice or Create/Update control after fresh
  pre-action verification.

Do not reuse stale coordinates across screens. Before every mutating tap or
text/key event, require exact package focus, expected screen identity, and
freshly resolved target bounds. After every event, inspect the visible state
before continuing. On unexpected focus, value, screen, duplicate, or mutation,
stop; do not compensate or retry blindly.

Permitted text values only:

```text
Runtime Dependent
2
Runtime Contact
```

For Android `input text`, spaces may be encoded with the already standard
`%s` form. No other text, Unicode input, shell interpolation, clipboard,
IME/keyboard change, accessibility service, or source instrumentation is
authorized.

## Step 1 — Create Runtime Dependent

On the preserved or freshly reproduced Add Dependent form:

1. verify the name field is empty and relationship has the accepted prefilled
   value;
2. tap the name field using fresh bounds and verify focus;
3. enter exact ASCII `Runtime Dependent` once;
4. verify the exact rendered name and unchanged relationship;
5. tap visible Create exactly once; and
6. prove exactly one `Runtime Dependent` exists and no duplicate was created.

If one already exists before input, do not create another; reuse exactly one.
If more than one exists or state is ambiguous, stop without delete/reset.

## Step 2 — Create And Edit Self Service Draft

Using accepted Control-owned navigation:

1. from Discover select `線上祈福服務` and create exactly one self draft at
   quantity `1` using accepted defaults;
2. prove one draft, immutable offering/`TWD 600`, and paid fixture retained;
3. reopen the self service draft and freshly verify offering, fee, self,
   quantity `1`, contact field, and Update control;
4. focus quantity, send one delete event only after verifying current value is
   exactly the single character `1`, then enter `2`;
5. focus the empty contact-name field and enter exact `Runtime Contact` once;
6. verify exact quantity `2`, exact contact name, and unchanged offering/fee;
7. tap Update exactly once;
8. prove one service draft/no duplicate/paid fixture retained; and
9. reopen and prove quantity `2`, `Runtime Contact`, `線上祈福服務`, and
   `TWD 600`, then return without another update.

If the contact field is not empty, quantity is not exactly `1`, or any
precondition differs, stop without clearing or saving.

## Step 3 — Create Dependent Blessing Draft

1. From Discover select `平安祈福` and verify immutable `TWD 1,200`, quantity
   `1`, all expected registrant choices, and no existing Runtime Dependent
   draft for that offering.
2. Resolve the current visible `Runtime Dependent` choice bounds from fresh UI
   evidence and tap it exactly once.
3. Verify Runtime Dependent selected, self not selected, and unchanged
   offering/fee/quantity.
4. Tap Create exactly once.
5. Prove exactly one new Runtime Dependent `平安祈福` draft, no duplicates,
   edited service draft retained, and paid fixture retained.
6. Reopen and prove dependent plus immutable offering/fee, then return without
   editing.

## Final Guard, Reset, And Cleanup

1. Reconfirm the paid fixture remains display-only with no payment/checkout.
2. Use visible dummy reset once.
3. Prove Runtime Dependent and both temporary drafts are removed and canonical
   one fixture dependent, one paid/read-only registration, and catalog return.
4. Stop only packet Metro if running.
5. Remove only exact serial `tcp:8081` reverse, temporary dependency symlink,
   and packet-created screenshots/UI hierarchy/evidence.
6. Prove no listener/reverse/symlink/evidence/Git/staging residue while
   preserving the installed development client and camera permission.

On an early stop, visible reset is allowed only from a known safely resettable
app state. Never clear package storage or blindly retry.

## Acceptance And Terminal

The immutable terminal records:

- reconnected serial/model/device and current USB location as observation only;
- preserved-session or reproduced-session classification;
- exact pre/post evidence for each bounded input/tap;
- one dependent, saved/reopened self draft, saved/reopened dependent draft,
  paid fixture, reset, and cleanup results;
- any first prevented criterion and JavaScript/native/evidence-method boundary;
  and
- confirmation that no Director input/request occurred.

Completion classification:
`expo_v1_registration_authority_runtime_validation_complete`.

Other accepted classifications remain:

- `expo_v1_registration_authority_runtime_defect_found`;
- `expo_v1_registration_authority_runtime_evidence_partial`;
- `runtime_cleanup_reconciliation_required`;
- `no_evidence_backed_direct_repair_remaining`.

`director_action_required` is not an accepted classification in this plan.

## Explicit Exclusions

- no Director/manual input or callback;
- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no clipboard, alternate IME/keyboard, accessibility setup, inspector,
  developer menu, source instrumentation, app/package storage clear,
  reinstall, or package mutation;
- no system unlock/status-bar/NotificationShade automation or non-TempleMate
  app interaction;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no local/EAS build, APK/AAB, signing, store/OTA, version/build change; retain
  `1.0.0 / Android 1 / iOS 1`;
- no Candidate B, SSH, production checkout, preflight, cleanup,
  release-ref, migration, restart, flag, provider-validation, or remediation.

Current classification:
`expo_v1_registration_authority_runtime_control_input_authorized`.

First blocker: none at dispatch. Control owns all permitted input and must not
request Director action.
