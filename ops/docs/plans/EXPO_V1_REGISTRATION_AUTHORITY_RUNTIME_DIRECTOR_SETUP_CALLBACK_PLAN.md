# Expo V1 Registration Authority Runtime Director Setup Callback Plan

Status: accepted for direct runtime dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base:
`474834b648fc879de28a05ee53a86741f528ad5f`

Accepted parent method:
EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_EVIDENCE_FOREGROUND_RETRY_PLAN.md
(deleted 2026-08-22 in the plans/archive cleanup; recoverable via
`git log --grep`)

Accepted planning-gap report:
`ops/docs/handoffs/2026-08-12-expo-v1-registration-authority-runtime-evidence-foreground-retry-control-b.md`

## Objective

Retry the same two registration evidence rows under every accepted parent-plan
criterion, adding only the missing Director callback needed to create the
temporary `Runtime Dependent` during deterministic setup.

The accepted report proved TempleMate foreground, exact local Metro
attachment, bundle, signed-in surface, reset, and cleanup. It also proved the
canonical fixture has no `Runtime Dependent`. The parent required that setup
while prohibiting ADB/clipboard/inspector/keyboard/accessibility input and
provided callbacks only after setup. This plan closes that planning-method gap;
it does not repair or change product source.

## Inherited Authority

All parent-plan entry fences, exact Pixel/package/version/API identity, USB
Metro method, wait-for-listener ordering, no-Metro-QR rule, registration
criteria, self-edit callback, dependent-choice callback, one-action saves,
reopen proof, paid-read-only guard, visible reset, cleanup, terminal schema,
classifications, and exclusions remain unchanged.

Control uses a fresh isolated codex-prefixed packet and at most one ephemeral
Implementer for report preparation/static review only. Control exclusively
owns Metro/ADB/device setup, visible verification, callbacks, mutations, reset,
cleanup, acceptance, local report integration, and terminal delivery.

## Conditional Foreground Gate

Before Metro, require the exact installed TempleMate package visibly
foreground and NotificationShade absent. If false, send only:

`director_action_required: templemate_foreground`

Resume only after Planning sends:

`director_action_complete: templemate_foreground`

Do not automate unlock, status bar, system navigation, or package launch.

## Callback 0 — Create Runtime Dependent

After normal signed-in dummy attachment/reset, first inspect the dependent
list:

- if exactly one `Runtime Dependent` already exists with the accepted
  relationship, reuse it and do not create another;
- if more than one exists or its state is ambiguous, stop without deletion;
- if absent, open the visible Add Dependent form and require an empty name
  field, the accepted prefilled relationship, and visible Create control.

When the absent case is prepared, Control sends exactly:

`director_action_required: registration_dependent_create_fields`

Planning instructs the Director to:

1. enter exact name `Runtime Dependent` in the already-open name field;
2. leave the prefilled relationship unchanged;
3. leave the form open; and
4. not press Create, Back, Cancel, or any developer/system control.

After the Director replies, Planning sends exactly:

`director_action_complete: registration_dependent_create_fields`

Control then visibly requires exact name `Runtime Dependent` and the accepted
relationship, presses Create exactly once, and proves exactly one resulting
dependent with no duplicate. If the name/relationship is not exact or the form
changed, classify setup `untested` and stop without save or normalization.

No automated text entry is permitted before or after this callback.

## Remaining Two Callbacks And Evidence

After exactly one Runtime Dependent exists, execute the parent plan unchanged:

1. create one self `線上祈福服務` draft at quantity `1` as setup;
2. prepare its edit form and send only
   `director_action_required: registration_self_edit_fields`;
3. after Planning sends
   `director_action_complete: registration_self_edit_fields`, visibly verify
   quantity `2` and `Runtime Contact`, update once, and prove one reopened
   draft with immutable `TWD 600` and no duplicate;
4. prepare `平安祈福` at `TWD 1,200` and send only
   `director_action_required: registration_dependent_choice`;
5. after Planning sends
   `director_action_complete: registration_dependent_choice`, visibly verify
   Runtime Dependent selected, create once, and prove one reopened dependent
   draft with immutable offering/fee and no duplicate; and
6. retain and reconfirm the paid self fixture as read-only with no
   payment/checkout action.

Then use visible reset once and prove canonical fixture restoration plus exact
Metro/reverse/symlink/evidence cleanup. Preserve the installed client and
camera permission.

## Explicit Exclusions

- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no ADB text/selection, clipboard, inspector, alternate keyboard,
  accessibility setup, app-storage/package mutation, reinstall, or source
  instrumentation;
- no status-bar/unlock/system-navigation/package-launch workaround;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no build/EAS/APK/AAB/signing/store/OTA/version/build change; retain
  `1.0.0 / Android 1 / iOS 1`;
- no production preflight/reconciliation, SSH, Candidate B, release ref,
  migration, restart, flag, provider-validation, or remediation action.

Current classification:
`expo_v1_registration_authority_runtime_director_setup_callback_authorized`.

First blocker: none at dispatch. Director input is requested only through the
three exact form callbacks or the conditional foreground callback.
