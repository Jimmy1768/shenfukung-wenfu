# Expo V1 Registration Runtime Keyboard-Dismissal Retry Plan

Status: accepted for direct runtime dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base:
`d9d8d1dedfdebe11ba2db06bc264bf55389a7362`

Accepted parent input plan:
`ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_CONTROL_INPUT_PLAN.md`

Accepted input-method report:
`ops/docs/handoffs/2026-08-12-expo-v1-registration-runtime-control-input-control-b.md`

## Diagnosis And Objective

The accepted attempt proved exact `Runtime Dependent` text entry, unchanged
relationship, and no record creation. The software keyboard remained active;
the intended Create interaction hit the IME's `y` key instead, producing
`Runtime Dependenty`. Control stopped immediately, reset canonical dummy state,
and cleaned up. This is a bounded evidence/input-method failure, not a product
source defect.

Retry the parent plan unchanged except for one required keyboard lifecycle:
after every authorized text-field edit, dismiss only the visible IME with one
Android Back event, prove TempleMate remains on the same form with the exact
value intact and the keyboard absent, then resolve the intended button from
fresh post-IME UI evidence before tapping it.

No Director input or callback is permitted.

## Entry And Runtime Authority

Control creates one fresh isolated codex-prefixed packet from the exact
committed plan base. At most one ephemeral Implementer may prepare/static-check
the report only. Control owns target-fenced Metro/ADB/device input, evidence,
reset, cleanup, acceptance, local report integration, and terminal delivery.

Retain all parent-plan requirements for:

- exact serial `39011FDJH00FQ8`, Pixel 8 / `shiba`, Komainu dev package,
  version/build/API identity, foreground/no-NotificationShade, dependency and
  repository checks;
- exact USB reverse/local Metro attachment and no Metro QR;
- permitted ASCII values only: `Runtime Dependent`, `2`, `Runtime Contact`;
- fresh package-scoped UI hierarchy/screenshot and newly resolved bounds before
  every mutation;
- dependent, self draft, dependent draft, paid fixture, reset, and cleanup
  criteria; and
- every explicit exclusion and stop-on-uncertainty rule.

The observed USB location is non-authoritative. Fence all device actions by
exact serial, package focus, expected screen, and fresh UI bounds.

## Required IME Lifecycle

For each authorized text field:

1. prove the expected TempleMate form and field's exact pre-edit value;
2. resolve current field bounds and tap once;
3. prove that field focused and the software keyboard is visibly present;
4. perform only the permitted ASCII entry/minimum key events;
5. prove the exact rendered value before leaving the field;
6. send exactly one target-fenced Android Back key event;
7. prove all of:
   - the software keyboard is absent;
   - `com.jimmy1768.komainu.dev/.MainActivity` remains foreground;
   - the same TempleMate form remains open;
   - the exact entered value remains unchanged; and
   - no Create/Update/navigation action occurred;
8. capture a fresh post-IME hierarchy/screenshot and resolve the intended
   Create or Update control again; and
9. tap the control once only after its fresh visible bounds no longer overlap
   the absent keyboard region.

If one Back closes/navigates the TempleMate form, the IME was not truthfully
shown, the value changes, or button bounds remain ambiguous, stop without
retry, correction, save, or another Back event.

Do not reuse any pre-dismissal button coordinate. Do not use an IME action key,
Enter, Tab, keyboard suggestion, keyboard hide command, global/system Back
after the IME is absent, or a coordinate in the former keyboard region.

## Step 1 — Runtime Dependent

From canonical reset state:

1. prove Runtime Dependent absent;
2. open Add Dependent and prove empty name, accepted prefilled relationship,
   and visible Create control;
3. enter exact `Runtime Dependent` once;
4. apply the required IME lifecycle;
5. after post-IME proof, freshly resolve and tap Create exactly once; and
6. prove exactly one Runtime Dependent and no duplicate.

If any extra character appears, stop immediately; do not delete or normalize.

## Step 2 — Self Service Draft

1. Create exactly one self `線上祈福服務` draft at quantity `1` using accepted
   defaults and prove immutable `TWD 600` plus paid fixture retained.
2. Reopen it and prove quantity exactly the single character `1` and contact
   field empty.
3. Edit quantity by one verified delete event followed by exact `2`, then apply
   the IME lifecycle without tapping Update yet.
4. Focus the still-empty contact field, enter exact `Runtime Contact`, and
   apply the IME lifecycle again.
5. After the second post-IME proof, freshly resolve Update, verify quantity
   `2`, `Runtime Contact`, offering and fee, and tap Update once.
6. Prove one draft/no duplicate/paid fixture retained, reopen, and prove saved
   quantity/contact plus immutable offering/fee.

The Back event after quantity must hide only the IME and preserve the edit
form; then contact entry repeats the same rule. If either Back changes screen,
stop without Update.

## Step 3 — Dependent Blessing Draft

Execute the parent plan unchanged: prepare `平安祈福` / `TWD 1,200` / quantity
`1`, freshly resolve and tap Runtime Dependent once, verify selection and
unchanged offering/fee/quantity, tap Create once, and prove/reopen exactly one
dependent draft with no duplicate while the paid fixture and edited service
draft remain.

No keyboard lifecycle is needed for the registrant choice because it is not a
text field. If an IME is unexpectedly visible, stop before selection.

## Final Guard And Cleanup

Reconfirm paid-read-only/no payment or checkout; use visible dummy reset once;
prove canonical fixture restoration; stop only packet Metro; remove exact
serial reverse, temporary dependency symlink, and packet UI evidence; and prove
no listener/reverse/symlink/evidence/Git/staging residue. Preserve installed
client and camera permission.

On early stop, reset only from a known safely resettable TempleMate state.
Never clear package storage or blindly retry.

## Terminal Evidence

The immutable report must explicitly record, for every text field:

- pre-edit value;
- exact post-entry rendered value;
- keyboard visible before the single Back;
- keyboard absent afterward;
- same form/package still foreground;
- exact value retained;
- post-IME button bounds freshly resolved; and
- single save/create outcome.

It also records the full parent registration rows, paid guard, reset, cleanup,
and confirmation that no Director action/request occurred.

Completion classification:
`expo_v1_registration_authority_runtime_validation_complete`.

Other accepted classifications:

- `expo_v1_registration_authority_runtime_defect_found`;
- `expo_v1_registration_authority_runtime_evidence_partial`;
- `runtime_cleanup_reconciliation_required`;
- `no_evidence_backed_direct_repair_remaining`.

## Explicit Exclusions

- no Director/manual callback or input;
- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no stale/pre-IME coordinate reuse, IME action key, Enter/Tab/suggestion,
  keyboard hide command, second/global Back, clipboard, alternate IME,
  accessibility, inspector/developer menu, instrumentation, package/storage
  mutation, reinstall, or system-UI workaround;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no build/EAS/APK/AAB/signing/store/OTA/version/build change; retain
  `1.0.0 / Android 1 / iOS 1`;
- no SSH/production checkout/Candidate B/preflight/cleanup/ref/migration/
  restart/flag/provider-validation/remediation action.

Current classification:
`expo_v1_registration_runtime_keyboard_dismissal_retry_authorized`.

First blocker: none at dispatch. Control owns the bounded keyboard-aware input
and must not request Director action.
