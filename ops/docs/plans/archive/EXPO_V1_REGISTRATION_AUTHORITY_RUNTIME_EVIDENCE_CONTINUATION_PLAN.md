# Expo V1 Registration Authority Runtime Evidence Continuation Plan

Status: accepted for direct runtime dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base:
`72e8476899197f28f6ac428851d2613fe236a83d`

Accepted predecessor plan:
`ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_VALIDATION_PLAN.md`

Accepted partial runtime report:
`ops/docs/handoffs/2026-08-12-expo-v1-registration-authority-runtime-validation-control-b.md`

Accepted source implementation:
`693e6dcbaa8525e05fea2b133dbed956d86d6537`

## Objective

Complete only the two runtime rows that remain untested after the accepted
partial report:

1. edit the self `線上祈福服務` draft to quantity `2` and contact name
   `Runtime Contact`, then prove the single draft retains the immutable
   offering and `TWD 600` fee; and
2. create one `平安祈福` draft for the temporary `Runtime Dependent`, then
   prove it coexists with the paid self fixture while retaining the immutable
   `TWD 1,200` fee.

All other predecessor rows are complete and must not be repeated except for
the minimum deterministic setup, entry fence, final reset, and cleanup needed
for these two rows.

This continuation changes only the evidence method. It does not change the
accepted product contract or authorize source repair.

## Accepted Prior Evidence

Retain the predecessor report as authority for:

- the signed-in account-only dummy shell;
- exact `Runtime Dependent` creation without concatenation or duplication;
- Registrations-to-Discover-only entry;
- all three temple-defined offerings and read-only TWD fees;
- preparation and Cancel without mutation;
- self service-draft creation at quantity `1`;
- paid fixture read-only/no payment or checkout;
- canonical reset restoration; and
- exact runtime and repository cleanup.

The two missing rows are `untested`, not defects. The prior Android
input/developer-inspector foreground behavior occurred before either save, and
later observation proved no partial mutation.

## Evidence-Method Decision

Target-fenced automation remains the default for navigation and safe visible
controls. It must not be used to enter the two problematic form values or to
select the dependent registrant in this continuation.

Instead, use two exact Director physical-input callbacks. The Director touches
only the already prepared TempleMate form and performs no Metro, developer
menu, scanner, provider, system-settings, install, or other action. Control B
owns form setup, visible-state verification, Save/Create actions, resulting
state review, reset, and cleanup.

Control must not substitute approximate text, clipboard injection, another
keyboard, ADB text, inspector interaction, accessibility-service setup, app
storage mutation, or source instrumentation.

## Entry Gate And Runtime Method

Control independently verifies:

- canonical source is clean at the committed plan base and contains accepted
  source implementation `693e6dc` plus accepted partial report `72e8476`;
- full mobile tests remain 51/51, followed by `yarn lint` and `yarn verify`;
- dependency manifests/lockfile are byte-identical to the accepted available
  dependency tree, with only the accepted temporary-symlink method if needed;
- exact Pixel serial `39011FDJH00FQ8`, Pixel 8 / `shiba`, installed
  `com.jimmy1768.komainu.dev`, `.MainActivity`, `1.0.0`/code `1`/target SDK
  36, and clean TCP 8081/reverse fences pass.

Use only:

    adb -s 39011FDJH00FQ8 reverse tcp:8081 tcp:8081
    TEMPLEMATE_CLIENT_MODE=dummy BUILD_MODE=development npx expo start --dev-client --localhost --port 8081

Open only Metro's emitted local `exp+templemate` URL through target-fenced ADB.
Never ask the Director to scan a Metro QR code. Do not use any QR/camera surface
in this packet.

## Deterministic Setup

1. Reach the signed-in dummy account shell and use visible reset if needed.
2. Recreate exactly one temporary dependent named `Runtime Dependent` while
   retaining the prefilled relationship. This is setup using the already
   accepted flow, not a new acceptance row.
3. From Discover, select `線上祈福服務` and create exactly one self draft at
   quantity `1` using accepted defaults. This is setup using the already
   accepted flow.
4. Require the paid fixture and one self service draft, with no duplicate.

If either accepted setup step cannot be reproduced without mutation ambiguity,
stop and report the exact first prevented criterion. Do not continue to the
Director callbacks from an uncertain state.

## Callback 1: Self-Draft Edit Fields

Control opens the exact self `線上祈福服務` draft and requires, before pause:

- offering `線上祈福服務`;
- read-only `TWD 600`;
- self registrant selected;
- quantity `1`; and
- the visible contact-name field and Update control.

Control then sends exactly:

`director_action_required: registration_self_edit_fields`

Planning will instruct the Director to perform only these actions on the
already open TempleMate screen:

1. clear the quantity field and enter exactly `2`;
2. clear the contact-name field and enter exactly `Runtime Contact`;
3. leave the form open; and
4. do not press Update, Back, Cancel, or any developer/system control.

After the Director replies that the input is complete, Planning sends Control
B exactly:

`director_action_complete: registration_self_edit_fields`

Control then:

1. visibly verifies exact quantity `2` and exact contact name
   `Runtime Contact` before mutation;
2. requires unchanged offering `線上祈福服務` and `TWD 600`;
3. presses Update exactly once;
4. requires one service draft with no duplicate and the paid fixture retained;
5. reopens the draft and requires quantity `2`, contact name
   `Runtime Contact`, and unchanged offering/fee; and
6. returns safely without another update.

If the Director-entered values are not exact or the app leaves the form before
Update, record this row `untested` and stop; do not repair, normalize, or save.

## Callback 2: Dependent Registrant Choice

Control returns to Discover, selects `平安祈福`, and requires before pause:

- offering `平安祈福`;
- read-only `TWD 1,200`;
- quantity `1`;
- fixture-self, fixture-dependent, and `Runtime Dependent` choices; and
- no existing unpaid/draft registration for `Runtime Dependent` and that
  offering.

Control then sends exactly:

`director_action_required: registration_dependent_choice`

Planning will instruct the Director to perform only:

1. tap the visible `Runtime Dependent` registrant choice once;
2. leave the prepared form open; and
3. do not press Create, Back, Cancel, or any developer/system control.

After the Director replies that the choice is complete, Planning sends Control
B exactly:

`director_action_complete: registration_dependent_choice`

Control then:

1. visibly verifies `Runtime Dependent` is selected and self is not selected;
2. requires unchanged `平安祈福`, `TWD 1,200`, and quantity `1`;
3. presses Create exactly once;
4. requires exactly one new `平安祈福` draft for `Runtime Dependent`;
5. requires the existing paid self fixture and edited service draft to remain,
   with no duplicates;
6. reopens the dependent draft and requires the selected dependent plus
   immutable offering/fee; and
7. returns safely without editing.

If the exact dependent is not visibly selected or the app leaves the form
before Create, record this row `untested` and stop; do not guess or save.

## Final Reset And Cleanup

After both rows pass:

1. reconfirm the paid fixture remains display-only with no payment/checkout
   action;
2. use visible dummy reset once;
3. require removal of `Runtime Dependent` and both temporary drafts and exact
   restoration of one fixture dependent, one paid/read-only registration, and
   the canonical catalog;
4. stop only packet Metro;
5. remove only exact serial `tcp:8081` reverse, temporary dependency symlink,
   and packet-created ephemeral evidence; and
6. prove no TCP 8081 listener, reverse, symlink, evidence, Git, or staging
   residue remains while preserving the installed client and camera permission.

If runtime stops before both rows pass, use visible reset only when the app is
in a safely resettable state, then perform the same exact cleanup. Record any
uncertain mutation or cleanup state explicitly; never blindly retry.

## Evidence And Acceptance

The immutable report records `passed`, `untested`, or `defect` for:

- exact Director-input self draft values before Update;
- one saved/reopened self draft with quantity `2`, `Runtime Contact`, immutable
  `線上祈福服務` / `TWD 600`, no duplicate, and paid fixture retained;
- exact Director-selected `Runtime Dependent` before Create;
- one saved/reopened dependent draft with immutable `平安祈福` /
  `TWD 1,200`, no duplicate, and paid self fixture retained;
- final paid-read-only guard, reset restoration, and exact cleanup.

Completion requires affirmative visible evidence for both saved/reopened
rows. A Director input/selection mismatch before save is `untested`, not a
product defect. A reproducible app behavior failure after exact compliant input
is a defect and must name the first prevented criterion and
JavaScript/native/unknown boundary.

Control may use one ephemeral Implementer only for immutable report
preparation/static checks. No Implementer may touch Metro, ADB, device, or
Director callbacks.

## Explicit Exclusions

- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no ADB text entry or dependent-choice automation after a callback form is
  prepared;
- no developer inspector/menu action, clipboard injection, keyboard change,
  accessibility setup, storage clear, reinstall, or package mutation;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no prebuild, local/EAS build, APK/AAB, signing, store/OTA, version/build
  change; retain `1.0.0 / Android 1 / iOS 1`;
- no OAuth Candidate B construction, production preflight, release-ref move,
  migration, restart, flag change, provider validation, or account remediation.

## Terminal Classifications

- `expo_v1_registration_authority_runtime_validation_complete`;
- `expo_v1_registration_authority_runtime_defect_found`;
- `expo_v1_registration_authority_runtime_evidence_partial`;
- `director_action_required`;
- `runtime_cleanup_reconciliation_required`;
- `no_evidence_backed_direct_repair_remaining`.

Current classification:
`expo_v1_registration_authority_runtime_evidence_continuation_authorized`.

First blocker: none at dispatch. Director input is requested only at the two
exact prepared form checkpoints.
