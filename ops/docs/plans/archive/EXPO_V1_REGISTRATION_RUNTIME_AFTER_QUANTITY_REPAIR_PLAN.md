# Expo V1 Registration Runtime After Quantity Repair Plan

Status: accepted for direct runtime dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base and accepted source repair:
`a26a48e66debce29c1146929f1d760ae839e595a`

Accepted source plan:
`ops/docs/plans/EXPO_V1_REGISTRATION_QUANTITY_INPUT_REPAIR_PLAN.md`

Retained runtime evidence:

- `ops/docs/handoffs/2026-08-12-expo-v1-registration-authority-runtime-validation-control-b.md`
- `ops/docs/handoffs/2026-08-12-expo-v1-registration-runtime-keyboard-dismissal-retry-control-b.md`
- `ops/docs/handoffs/2026-08-12-expo-v1-registration-runtime-known-contact-default-retry-control-b.md`

## Objective

Validate the accepted JavaScript quantity repair in the already installed
development client and complete only the remaining registration-authority rows:

1. create exactly one Runtime Dependent;
2. create one self `線上祈福服務` draft with canonical default `林小安`;
3. edit and reopen it at quantity `2` and contact `Runtime Contact`, retaining
   immutable `TWD 600` and no duplicate;
4. create and reopen one Runtime Dependent `平安祈福` draft, retaining immutable
   `TWD 1,200` and no duplicate;
5. retain the paid fixture as read-only/no checkout; and
6. reset canonical dummy state and complete exact cleanup.

No native rebuild is needed or authorized. The installed Expo development
client receives the accepted JavaScript repair through the existing local USB
Metro method.

## Entry And Control Authority

Control creates one fresh isolated codex-prefixed packet. At most one ephemeral
Implementer may prepare/static-review the report only. Control owns all
target-fenced Metro/ADB/device/input/save/reset/cleanup/integration work. No
Director callback or manual input is permitted.

Before runtime require:

- exact source repair `a26a48e` and helper consumer present;
- mobile tests 52/52, `yarn lint`, and `yarn verify` pass;
- dependency package/lock tree matches the accepted available tree;
- exact serial `39011FDJH00FQ8`, Pixel 8 / `shiba`, installed
  `com.jimmy1768.komainu.dev`, `.MainActivity`, version `1.0.0`, code `1`,
  target SDK 36;
- clean listener/reverse entry fences and attributable repositories; and
- no native build/config/dependency/version change after the installed client.

Use only:

```text
adb -s 39011FDJH00FQ8 reverse tcp:8081 tcp:8081
TEMPLEMATE_CLIENT_MODE=dummy BUILD_MODE=development npx expo start --dev-client --localhost --port 8081
```

Wait for the exact listener and Metro-ready state before opening the emitted
local `exp+templemate` URL through exact-serial ADB. Never use or request a
Metro QR scan.

## Input Method

Retain the accepted Control-owned method:

- exact serial/package/screen/fresh package-scoped UI evidence before every
  mutation;
- newly resolved bounds for every tap, never stale coordinates;
- inserted text only `Runtime Dependent`, `2`, and `Runtime Contact`;
- `KEYCODE_MOVE_END` and exact deletions only after proving the exact known
  current value;
- after every inserted value, prove IME visible, send one Back, prove IME
  absent with same form/package and exact value retained, then refresh UI
  evidence before Create/Update; and
- stop on any unexpected focus/value/screen/duplicate/mutation without blind
  retry or correction.

## Runtime Sequence

### 1. Canonical setup

Use visible reset if needed. Prove canonical one fixture dependent, one paid
registration, catalog, and Runtime Dependent absent.

Create exactly one Runtime Dependent using the already passed keyboard-aware
path: empty name, accepted prefilled relationship, exact entry, one-Back IME
proof, fresh Create bounds, one Create, no duplicate.

### 2. Self draft create

From Discover select `線上祈福服務`. Require self, immutable `TWD 600`,
quantity `1`, and exact canonical contact `林小安`. With IME absent, freshly
resolve and tap Create once. Prove exactly one self draft and paid fixture
retained.

### 3. Quantity repair proof and self draft update

Reopen the self draft and require exact offering/fee/self, quantity `1`, and
contact `林小安`.

For quantity:

1. freshly focus and prove IME visible plus exact `1`;
2. send `KEYCODE_MOVE_END` once and `KEYCODE_DEL` exactly once;
3. require the field visibly remains empty—this is the direct repair proof;
4. enter `2` once; and
5. apply the complete one-Back IME lifecycle and prove `2` retained.

For contact:

1. freshly focus and prove IME visible plus exact `林小安`;
2. send `KEYCODE_MOVE_END` once and `KEYCODE_DEL` exactly three times;
3. require exact empty;
4. enter `Runtime Contact` once; and
5. apply the complete one-Back IME lifecycle and prove exact value retained.

Then require quantity `2`, `Runtime Contact`, unchanged offering/fee/self;
freshly resolve Update; tap once; prove one draft/no duplicate/paid fixture;
reopen and prove all saved values plus immutable authority.

### 4. Dependent blessing draft

From Discover select `平安祈福`. Require immutable `TWD 1,200`, quantity `1`,
all registrant choices, and no Runtime Dependent pair. With IME absent, freshly
resolve and tap Runtime Dependent once; verify it selected and self not
selected; freshly resolve Create and tap once; prove exactly one dependent
draft/no duplicate with edited service draft and paid fixture retained; reopen
and prove dependent plus immutable offering/fee.

### 5. Final guard and cleanup

Reconfirm paid display-only/no payment or checkout. Use visible dummy reset
once. Prove Runtime Dependent and both temporary drafts removed and canonical
fixture dependent, paid registration, and catalog restored. Stop only packet
Metro; remove exact serial reverse, temporary dependency symlink, and packet UI
evidence; prove no listener/reverse/symlink/evidence/Git/staging residue while
preserving installed client and camera permission.

## Acceptance And Terminal

Completion requires affirmative visible evidence for:

- repaired quantity field retaining empty after one exact delete;
- saved/reopened self draft quantity `2` and `Runtime Contact` with immutable
  `TWD 600` and no duplicate;
- saved/reopened Runtime Dependent draft with immutable `TWD 1,200` and no
  duplicate;
- paid-read-only, reset, and exact cleanup; and
- no Director input/request.

Completion classification:
`expo_v1_registration_authority_runtime_validation_complete`.

Other accepted classifications:

- `expo_v1_registration_authority_runtime_defect_found`;
- `expo_v1_registration_authority_runtime_evidence_partial`;
- `runtime_cleanup_reconciliation_required`;
- `no_evidence_backed_direct_repair_remaining`.

## Explicit Exclusions

- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no Director/manual input;
- no stale coordinates, extra deletion, select-all, clipboard, alternate IME,
  accessibility, inspector/developer menu, instrumentation, storage/package
  mutation, reinstall, or system-UI workaround;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no local/EAS build, APK/AAB, signing, store/OTA, version/build change;
  retain `1.0.0 / Android 1 / iOS 1`;
- no SSH/production checkout/Candidate B/preflight/cleanup/ref/migration/
  restart/flag/provider-validation/remediation action.

Current classification:
`expo_v1_registration_runtime_after_quantity_repair_authorized`.

First blocker: none at dispatch.
