# Expo V1 Registration Authority Runtime Evidence Foreground Retry Plan

Status: accepted for direct runtime dispatch to Control B after commit

Created: 2026-08-12

Owner: Wenfu Planning

Target: Wenfu Control B
`019fe020-e92e-7770-984f-b59acd547ab0`

Repository: `/Users/jimmy1768/Projects/shengfukung-wenfu`

Canonical Planning base:
`9882838`

Accepted source implementation:
`693e6dcbaa8525e05fea2b133dbed956d86d6537`

Accepted partial runtime evidence:
`72e8476899197f28f6ac428851d2613fe236a83d`

Accepted foreground-limited report:
`eb41760d72be0e7b366f80587bbb3b3987156b2f`

Predecessor evidence-method plan:
`ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_EVIDENCE_CONTINUATION_PLAN.md`

## Changed Prerequisite And Objective

The Director confirms exact Pixel serial `39011FDJH00FQ8` is unlocked, the
NotificationShade is fully dismissed, and TempleMate's installed development
client is open and visible. Control B has taken no new runtime action since the
accepted foreground-limited report.

Use that changed prerequisite to complete only the same two untested rows:

1. edit the self `線上祈福服務` draft to quantity `2` and contact name
   `Runtime Contact`, then prove one saved/reopened draft retains the immutable
   offering and `TWD 600` fee; and
2. create one `平安祈福` draft for the temporary `Runtime Dependent`, then
   prove one saved/reopened dependent draft retains the immutable offering and
   `TWD 1,200` fee while the paid self fixture remains read-only.

This is an evidence-method retry, not a source repair. Retain every accepted
predecessor row and do not repeat unrelated account, OAuth, camera, QR, tenant,
UI, or registration checks.

## Control Method And Entry Fence

Control creates one isolated codex-prefixed branch/worktree from the exact
committed plan base and records one immutable runtime packet. At most one
ephemeral Implementer may prepare/static-check the report only. Control owns
Metro, ADB, target verification, visible-state setup, Director callbacks,
save/create actions, evidence review, reset, cleanup, and terminal delivery.

Before any Metro/reverse mutation, independently require:

- canonical/isolated clean state and ancestry of `693e6dc`, `72e8476`, and
  `eb41760`;
- mobile tests remain 51/51, followed by `yarn lint` and `yarn verify`;
- dependency package/lock tree remains byte-identical to the accepted local
  dependency tree, using only the previously accepted temporary symlink if
  required and removing it during cleanup;
- exact connected Pixel serial `39011FDJH00FQ8`, Pixel 8 / `shiba`, installed
  `com.jimmy1768.komainu.dev`, `.MainActivity`, version `1.0.0`, code `1`,
  target SDK 36;
- no TCP 8081 listener and no exact-device `tcp:8081` reverse; and
- target-fenced read-only activity/window evidence shows the unlocked visible
  foreground belongs to `com.jimmy1768.komainu.dev` and NotificationShade is
  not the focused surface.

If the last foreground condition is not true at entry, send exactly
`director_action_required: templemate_foreground`, pause without starting
Metro or changing the device, and resume only after Planning relays
`director_action_complete: templemate_foreground`. Do not use status-bar,
unlock, system-navigation, package-launch, or notification automation as a
substitute.

## Exact USB Metro Attachment

Use only the previously accepted method:

```text
adb -s 39011FDJH00FQ8 reverse tcp:8081 tcp:8081
TEMPLEMATE_CLIENT_MODE=dummy BUILD_MODE=development npx expo start --dev-client --localhost --port 8081
```

Wait until the exact local TCP listener is present and Metro reports ready
before sending Metro's emitted local `exp+templemate` URL through target-fenced
ADB. Never ask the Director to scan a Metro/Expo QR code. After attachment,
require a successful bundle, TempleMate foreground, and a normal app surface.
Stop on a developer overlay, NotificationShade, transport/render error, or
uncertain foreground; do not dismiss or repair it inside this packet.

## Minimum Deterministic Setup

1. Reach the signed-in account-only dummy shell and use visible reset if
   needed.
2. Recreate exactly one temporary dependent named `Runtime Dependent`,
   retaining the prefilled relationship.
3. From Discover, select `線上祈福服務` and create exactly one self draft at
   quantity `1` using accepted defaults.
4. Require one paid/read-only fixture, one self service draft, one Runtime
   Dependent, and no duplicates.

These are accepted setup flows, not new acceptance rows. If setup cannot be
reproduced deterministically, stop before either Director callback and report
the first prevented criterion. Do not use ADB text, clipboard, inspector,
keyboard/accessibility changes, app-storage mutation, or approximate values.

## Director Callback 1 — Self Draft Fields

Control opens the exact self `線上祈福服務` draft and visibly requires:

- offering `線上祈福服務` and read-only `TWD 600`;
- self selected;
- quantity `1`; and
- visible contact-name and Update controls.

Then send exactly:

`director_action_required: registration_self_edit_fields`

Planning instructs the Director to clear and enter exact quantity `2`, clear
and enter exact contact name `Runtime Contact`, leave the form open, and not
press Update, Back, Cancel, or any developer/system control. After the Director
confirms, Planning sends exactly:

`director_action_complete: registration_self_edit_fields`

Control then requires exact visible values and unchanged offering/fee, presses
Update once, proves one draft/no duplicate/paid fixture retained, reopens it,
and proves quantity `2`, `Runtime Contact`, `線上祈福服務`, and `TWD 600`.
Return without another update.

If the exact values are absent or the form changed before save, classify the
row `untested` and stop without normalization or save.

## Director Callback 2 — Dependent Choice

Control returns to Discover, selects `平安祈福`, and visibly requires:

- offering `平安祈福` and read-only `TWD 1,200`;
- quantity `1`;
- fixture-self, fixture-dependent, and `Runtime Dependent` choices; and
- no existing draft for that dependent/offering pair.

Then send exactly:

`director_action_required: registration_dependent_choice`

Planning instructs the Director to tap visible `Runtime Dependent` once, leave
the form open, and not press Create, Back, Cancel, or any developer/system
control. After the Director confirms, Planning sends exactly:

`director_action_complete: registration_dependent_choice`

Control then requires Runtime Dependent selected and self not selected,
unchanged offering/fee/quantity, presses Create once, proves exactly one new
dependent draft with no duplicates and the paid fixture plus edited service
draft retained, reopens it, and proves dependent/offering/fee immutability.
Return without editing.

If the exact dependent is not selected or the form changed before save,
classify the row `untested` and stop without guessing or creating.

## Final Guard, Reset, And Cleanup

After both rows pass:

1. reconfirm the paid fixture is display-only with no payment/checkout action;
2. use visible dummy reset once;
3. require removal of Runtime Dependent and both temporary drafts, restoring
   one fixture dependent, one paid/read-only registration, and canonical
   catalog;
4. stop only packet Metro;
5. remove only the exact serial `tcp:8081` reverse, temporary dependency
   symlink, and packet-created ephemeral UI evidence; and
6. prove no TCP 8081 listener, reverse, symlink, evidence, Git, or staging
   residue while preserving the installed client and camera permission.

On an early stop, reset only from a visibly safe resettable app state. Never
blindly retry or clear app/package storage. Record any uncertain mutation or
cleanup state explicitly.

## Acceptance And Terminal

The immutable report records `passed`, `untested`, or `defect` for:

- foreground/readiness and exact Metro attachment;
- exact Director-input self values before Update;
- saved/reopened self draft with quantity `2`, `Runtime Contact`, immutable
  offering/fee, no duplicate, and paid fixture retained;
- exact Director-selected dependent before Create;
- saved/reopened dependent draft with immutable offering/fee, no duplicate,
  and paid fixture retained; and
- paid guard, reset restoration, and exact cleanup.

Completion requires affirmative visible evidence for both saved/reopened rows.
Director input mismatch before save is `untested`; reproducible app failure
after exact compliant input is a defect and must identify the first prevented
criterion and JavaScript/native/unknown boundary.

Accepted terminal classifications:

- `expo_v1_registration_authority_runtime_validation_complete`;
- `expo_v1_registration_authority_runtime_defect_found`;
- `expo_v1_registration_authority_runtime_evidence_partial`;
- `director_action_required`;
- `runtime_cleanup_reconciliation_required`;
- `no_evidence_backed_direct_repair_remaining`.

## Explicit Exclusions

- no source/config/test/dependency/lockfile/native/Rails/Vue/Planning repair;
- no ADB text or dependent-choice automation after a callback form is ready;
- no status-bar/unlock/system-navigation/package-launch workaround, developer
  overlay/menu/inspector, clipboard, keyboard/accessibility, storage clear,
  reinstall, or package mutation;
- no QR/camera, tenant switch, real API/OAuth/provider/browser, payment,
  admin, production, deployment, release, push, or secret action;
- no prebuild, local/EAS build, APK/AAB, signing, store/OTA, version/build
  change; retain `1.0.0 / Android 1 / iOS 1`;
- no Candidate B construction, production preflight, SSH, release-ref move,
  migration, restart, flag change, provider validation, or remediation.

Current classification:
`expo_v1_registration_authority_runtime_foreground_retry_authorized`.

First blocker: none at dispatch. Director interaction is requested only through
the exact callbacks above.
