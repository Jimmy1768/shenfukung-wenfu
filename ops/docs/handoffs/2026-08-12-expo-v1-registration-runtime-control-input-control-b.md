# Expo V1 registration runtime Control input — Control packet

## Immutable identity

- Plan/base: `ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_CONTROL_INPUT_PLAN.md` at `4e09eaec23e2238bed581d3186c1afdc50ef0cf8`.
- Accepted source/evidence ancestry: `693e6dcbaa8525e05fea2b133dbed956d86d6537`, `72e8476899197f28f6ac428851d2613fe236a83d`, and `5123eee9a58aa2da53ae9f5bd5355623fa4ce3b4`.
- Control B `019fe020-e92e-7770-984f-b59acd547ab0` to Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Worktree/branch: `/private/tmp/shengfukung-wenfu-expo-v1-registration-runtime-control-input`; `codex/expo-v1-registration-runtime-control-input`.
- Packet/attempt: `2026-08-12-expo-v1-registration-runtime-control-input-control-b`, attempt 15.

## Authority and fences

- Mutate only exact Pixel serial `39011FDJH00FQ8` against TempleMate package `com.jimmy1768.komainu.dev`; record USB location as observed evidence only. Use a fresh hierarchy/screenshot and current bounds before every tap, text, or permitted edit key event.
- Continue the preserved Add Dependent form only if all new plan entry fences prove it exact. Otherwise clean/reproduce once through accepted runtime method.
- Allowed input only: `Runtime Dependent`, `2`, `Runtime Contact`. ADB `input text` and minimum verified edit/navigation keys are authorized only on visibly focused expected controls. No stale coordinates, blind retry, callback, or other text.
- Exact mutations: one dependent Create, one self service-draft Update, one dependent blessing-draft Create. After every event inspect current state; stop on unexpected focus/value/screen/mutation.
- No source/config/test/dependency/native/Rails/Vue/plan edits other than this report; no system UI workaround, clipboard/IME/accessibility/inspector/developer menu/storage/package, QR/camera/tenant/OAuth/provider/payment/admin, build/EAS/install/version, production/deploy/release/push/secret work.

## Execution and evidence

- Control uses `gpt-5.6-terra/high`; one normal ephemeral `gpt-5.6-terra/medium` Implementer prepares/static-checks this report only. Medium is sufficient because it performs no semantic implementation or retained-state mutation.
- Control owns all Pixel/Metro, bounded input/selection, verification, reset, cleanup, acceptance, commit, and direct terminal work.
- Required: canonical/isolated attribution/ancestry; 51/51 tests/lint/verify/dependency equivalence; serial/device/package/foreground/Metro fences; fresh pre/post proof per action; exact saved/reopened self and dependent draft proof; paid fixture; reset/cleanup; diff/status/staging.

## Sanitized runtime result matrix

Control observed the following runtime evidence. Implementer output is static only.

| Evidence | Status | Safe result / limitation |
| --- | --- | --- |
| Preserved-session entry and runtime fences | passed | Base ancestry included the named source and accepted reports; 51/51 tests/lint/verify and byte-identical dependencies passed. Exact Pixel 8 / `shiba` serial, package version/code/target, foreground/no-shade, listener/local attach, bundle, and normal signed-out then signed-in dummy surface passed. The stale preserved form was safely replaced by one clean reproduction. |
| Exact temporary dependent creation | partial / stopped | Fresh form proved empty name, relationship `家人`, visible Create, and no Runtime Dependent. Fresh name tap/focus, exact `Runtime Dependent` input, and exact render with unchanged relationship passed. A freshly resolved Create tap instead changed the field to `Runtime Dependenty`; no dependent was created. Control stopped immediately without compensation or retry. |
| Self service draft create/edit/save/reopen | not attempted | Prohibited after the mismatched post-action value. |
| Dependent blessing draft select/save/reopen | not attempted | Prohibited after the mismatched post-action value. |
| Paid guard, reset, cleanup | passed | No registration, selection, payment, or paid-fixture action occurred. Visible reset restored the canonical unbound dummy state; packet Metro stopped, reverse/listener absent, temporary symlink and named hierarchy evidence removed, and the installed client remained untouched. |

## Implementer static receipt

- Static packet review completed against the accepted control-input plan at base `4e09eaec23e2238bed581d3186c1afdc50ef0cf8`; no runtime, device, Metro, test, or external action was performed.
- Confirmed ancestry of the base includes source `693e6dcbaa8525e05fea2b133dbed956d86d6537` and accepted reports `72e8476899197f28f6ac428851d2613fe236a83d` and `5123eee9a58aa2da53ae9f5bd5355623fa4ce3b4`.
- Confirmed the bounded method: fresh package-scoped evidence and newly resolved bounds immediately before every mutating field/control event; ASCII-only input is limited to `Runtime Dependent`, `2`, and `Runtime Contact`; unexpected state requires a stop without retry.
- Static scope result: this report is the only Implementer-owned path; existing Control-packet sections are preserved. Git checks are clean except for this intended untracked report; no staging or commit was performed.

## Control closeout

- Classification: `expo_v1_registration_authority_runtime_evidence_partial`.
- Continuation disposition: `no_evidence_backed_direct_repair_remaining`.
- First prevented action: the freshly resolved Create-control tap did not submit the verified exact name; its post-action value became `Runtime Dependenty`. This unexpected value/screen interaction required an immediate stop under the accepted plan.
- Boundary: runtime evidence/control interaction only; this is not a product or source defect. No Director action was requested.
- Next owner/action: Planning must decide an accepted evidence method for the control-tap versus field-focus defect.
- Final local state: visible dummy reset complete; Metro, exact serial reverse/listener, temporary symlink, and named hierarchy evidence absent; installed development client retained. Git staging is empty and this report is the only untracked path.
