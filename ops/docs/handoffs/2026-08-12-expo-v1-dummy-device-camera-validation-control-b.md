# TempleMate dummy device and camera validation — Control packet and safe receipt

## Identity

- Accepted-plan path and immutable criteria:
  `ops/docs/plans/EXPO_V1_DUMMY_DEVICE_AND_CAMERA_VALIDATION_PLAN.md`, committed
  at `2581686d05ca805a772ae207acf09107b086c64e`.
- Control task and authority state: Wenfu Control B
  `019fe020-e92e-7770-984f-b59acd547ab0`; direct Planning dispatch; active.
- Repository, worktree, branch, and base HEAD:
  `/Users/jimmy1768/Projects/shengfukung-wenfu`,
  `/private/tmp/shengfukung-wenfu-expo-v1-dummy-device-camera-validation`,
  `codex/expo-v1-dummy-device-camera-validation`,
  `2581686d05ca805a772ae207acf09107b086c64e`.
- Packet status and date: frozen, 2026-08-12.
- Immutable packet identity and implementation attempt:
  `2026-08-12-expo-v1-dummy-device-camera-validation-control-b`, attempt 1.

## Scope

- Objective: attach only the installed Komainu development client on the exact
  Pixel to one localhost dummy Metro process, classify the bounded dummy,
  OAuth, camera, fixture-QR, and tenant-switch paths, and clean up all
  packet-created runtime residue without repairing source.
- Exact owned editable path:
  `ops/docs/handoffs/2026-08-12-expo-v1-dummy-device-camera-validation-control-b.md`.
- Explicitly excluded paths and systems: all product/config/test/dependency and
  version paths; Rails/Vue; EAS/build/signing/artifacts; real API/OAuth,
  provider, payment, deployment, release, and push. Device authority is fenced
  to serial `39011FDJH00FQ8`, package `com.jimmy1768.komainu.dev`, one
  localhost Metro process, and only `tcp:8081` ADB reverse.
- Required checks and expected evidence: exact Git/package/device preflight;
  byte-identical manifest and lockfile; temporary isolated dependency symlink;
  `yarn test`, `yarn lint`, `yarn verify`; explicit dummy Metro URL; targeted
  app-scoped UI evidence; camera/OAuth/account classifications; exact process,
  reverse, symlink, and ephemeral-evidence cleanup; diff and clean status.
- Evidence sources and status: accepted plan and installed receipt are
  documented; canonical base and clean state are Control-observed; device,
  Metro, UI, and cleanup receipts are Control-observed pending.
- First blocked surface, if known: none.

## Incident-Correction Placement

- Is this an incident correction? no; this is a runtime-observation packet.
- Selected surface: a single Control receipt; no product or governance source
  change is authorized.
- `AGENTS.md` excluded unless explicit Director authorization is recorded: no
  authorization exists.

## Repair And Terminal Boundary

- Is this a bounded nonterminal repair within unchanged immutable criteria: no.
- Failed attempt identity and evidence: none.
- Immutable repair packet direct mechanism, owned paths, and checks: not
  applicable.
- True Planning design gap, Director authority decision, or no evidence-backed
  direct repair remaining: terminal classification will be evidence-based; no
  repair may be made in this packet.
- Planning packet prohibited until a terminal disposition: confirmed.

## Handoff Eligibility (Before Model Selection)

- Persistent Handoff requested: no.
- If yes, exceptional recorded continuity reason and one-packet boundary: not
  applicable.
- Eligibility confirmed before selecting a model: no persistent handoff is
  eligible or used.
- Luna disqualifiers checked: availability, cost, mechanical simplicity, and
  rejection do not qualify: confirmed.

## Implementer Dispatch

- Selected model and reasoning: `gpt-5.6-terra/medium`.
- Selection reason and lowest-sufficient configuration: bounded report/evidence
  preparation only; it has no external or device authority.
- Ephemeral allocation: `gpt-5.6-terra/medium`; no retained-state mutation,
  source repair, or semantic implementation is delegated.
- One ephemeral Implementer task: create the report scaffold at the sole owned
  path; identify source-backed evidence fields and safe receipts only.
- Return destination: this Control directly.
- Implementer boundaries: this document only; no acceptance, staging, commit,
  merge, push, deploy, approval handling, secret access, external mutation,
  symlink, Metro, or ADB action.

## Control Review And Closeout

- Conformance review against immutable criteria: pending runtime execution and
  independent receipt review.
- Acceptance decision and rationale: pending.
- Integration, staging, and commit evidence when accepted: pending.
- Immutable terminal packet direct delivery, source Control, target Planning,
  implementation attempt, and continuation disposition: pending direct send to
  Wenfu Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Paired Planning receipt: `released_terminal_idle`, pending.
- Parent classification, continuation disposition, and next owner/action:
  pending Planning receipt.
- `active_packet: none` only with the exact missing decision and owner: not
  applicable while this packet is active.
- Residual risk, production gap, and next owner: real API/OAuth and production
  actions remain separately unauthorized.
- Authority confirmation: Planning reported evidence only; Strategy owns any
  cross-repository policy and the Director accepts it.

## Runtime Receipt (Control-observed)

All runtime, UI, and cleanup findings are pending. This document will record
only sanitized, app-scoped evidence and no camera media, credentials, private
device data, broad logs, providers, or external URLs.

### Safe preflight receipt

Control records only these bounded outcomes before runtime attachment:

| Assertion | Safe result field |
| --- | --- |
| Canonical and isolated source state | base/HEAD ancestry, clean status, and empty staging |
| Exact target fence | expected serial/model/codename/state and Android release/API match |
| Installed client fence | package name, public label, version name/code, target SDK, and launcher match |
| Localhost ownership | TCP 8081 unowned before Metro; exact target has no pre-existing `tcp:8081` reverse |
| Dependency identity | `mobile/package.json` and `mobile/yarn.lock` byte-identical to the accepted camera-source dependency tree; source `node_modules` available; isolated path absent before the temporary symlink |
| Static regression commands | pass/fail/exit classification for `yarn test`, `yarn lint`, and `yarn verify`, with aggregate test counts only |

No raw package-manager output, unrelated process information, device data, or
dependency-tree path outside this packet is retained.

### Dummy attachment receipt

Control records that one explicit dummy-only Metro process started from the
isolated mobile directory; its safe development-client URL was emitted and used
only for the fenced Komainu package; and exactly the target `tcp:8081` reverse
was present while attached. The receipt must state whether the rendered client
showed dummy mode with no API base URL or tenant slug. It must never reproduce
the URL, environment values beyond `dummy`, process arguments, local IPs, or
logs.

An unexpected real-mode indicator, request, provider browser, or
source-generation prompt is recorded as a named hard failure only, without
capturing request, provider, or URL data.

### Runtime assertion matrix

Each row is recorded as `passed`, `failed`, or `untested`, followed only by a
short sanitized observation and, when untested, the direct reason. A failed
row additionally classifies the likely follow-up surface as
`javascript_only`, `native_build_or_config`, or `unknown`; it is not repaired
in this packet.

| Area | Exact assertion | Result / safe observation / follow-up surface |
| --- | --- | --- |
| Signed-out account | TempleMate identity and explicit demo-mode presentation render | pending |
| Email sign-in | Invalid credentials reject; fixture email sign-in reaches the account-only signed-in state | pending |
| Scope | Account-only navigation has no admin surface | pending |
| Profile | Visible name edit completes | pending |
| Dependents | Create, edit, and delete complete | pending |
| Registrations | Create and edit complete; paid fixture is read-only and has no payment CTA | pending |
| Account content | Certificates, events, services, and gallery render | pending |
| Preferences | zh-TW/en and light/dark apply; no admin preference appears | pending |
| Dummy requests | Assistance, contact-temple, and privacy submissions complete in dummy mode | pending |
| Reset | Reset/sign-out returns to deterministic signed-out state | pending |
| Google dummy | One deterministic success remains network-free, returns account-only signed-in state, and clears on sign-out | pending |
| Apple dummy | One deterministic success remains network-free, returns account-only signed-in state, and clears on sign-out | pending |
| Camera denial | First scanner opening has one initial prompt; one denial shows visible denied/retry state and does not loop | pending |
| Camera retry/grant | Visible Retry produces one new request; grant reaches rear QR-only preview with no microphone/audio prompt | pending |
| Untrusted fixture QR | Untrusted scan is invalid/untrusted and leaves current binding unchanged | pending |
| Trusted fixture QR | One trusted scan consumes once, closes camera, and binds the named deterministic fixture tenant | pending |
| Tenant switch | Confirmation is visible; cancellation preserves binding; confirmation switches only after cleanup to the deterministic second fixture | pending |

### UI-refinement observations

Runtime observation is distinct from visual acceptance. Control records only
short app-scoped findings under these headings, each with the affected screen
and reproduction step: layout/overflow; keyboard and focus; scrolling;
edge-to-edge or system bars; touch targets; copy/localization; navigation; and
accessibility or theme contrast. Absence of a recorded issue is not final UI
approval.

### Physical QR evidence boundary

The physical Pixel may be pointed by the Director at the two Planning-provided
fixture images. Control records only the visible post-scan app classification;
it does not save camera media, QR images, frame data, or media-derived output.
If physical positioning does not occur during the bounded session, both fixture
rows are marked `untested: physical_qr_scan_unconfirmed` without simulated
success.

### Cleanup receipt

Control records only whether each packet-created item was removed: the one
Metro process, exact target `tcp:8081` reverse, temporary isolated
`node_modules` symlink, and packet-created screenshots/UI dumps. It then records
clean/empty-staging status for canonical and isolated worktrees, and the
terminal classification from the accepted plan. The installed app and its
accepted dummy/camera state are explicitly retained.

## Supersession Closeout

The Director superseded the Metro attachment method before any development-client
URL was used, package was attached or reloaded, or app UI, account, OAuth,
camera, QR, or tenant-switch interaction occurred. No runtime assertion has an
acceptance result.

- **Completed preflight:** canonical `main` and this isolated branch began at
  the accepted plan commit with empty staging; the exact Pixel/package/launcher
  identity matched; port 8081 and the target reverse mapping were initially
  unowned; manifests and lockfiles were byte-identical to the accepted existing
  dependency tree; and isolated `node_modules` was absent.
- **Permitted setup before supersession:** the exact temporary dependency
  symlink was created; `yarn test` passed 42/42; `yarn lint` and `yarn verify`
  passed; the exact serial-only `tcp:8081` reverse was created; and one explicit
  dummy localhost Metro process began. No safe development-client URL was
  emitted or used before the stop instruction.
- **No forbidden runtime action:** no package launch/attach/reload, UI input,
  screenshot/UI dump, camera-media access, provider browser, real API/OAuth,
  device package mutation, source edit, build, EAS action, or push occurred.
- **Exact cleanup:** the packet-owned Metro process had already exited when
  reconciliation was performed; only the exact `tcp:8081` reverse was removed;
  only the packet-created isolated `node_modules` symlink was removed; and no
  packet-created screenshots or UI dumps existed. Port 8081 has no listener
  and the target reverse list is empty.

The superseding direction arrived after the setup commands, but before the
first runtime acceptance boundary. It is therefore recorded truthfully as
`runtime_plan_superseded_before_runtime_acceptance`, not as validation success
or failure.

## Terminal

- Conformance decision: stopped and cleaned up under the Director override.
- Terminal classification: `runtime_plan_superseded_before_runtime_acceptance`.
- Continuation disposition: `director_decision_or_authority`.
- Integration: no canonical integration was attempted. At cleanup, canonical
  `main` contained unrelated modified Planning/current-coordination documents;
  they were preserved exactly. This isolated branch contains only this Control
  record and remains ready to commit as immutable terminal evidence.
- Next owner/action: Wenfu Planning must commit and directly dispatch the
  corrected attachment method before any renewed Metro, ADB reverse, attach,
  reload, or app interaction.
- Authority confirmation: no source repair, external provider/server action,
  build, release, or push was performed.
