# Expo V1 registration runtime keyboard dismissal retry — Control packet

## Immutable identity

- Plan/base: `ops/docs/plans/EXPO_V1_REGISTRATION_RUNTIME_KEYBOARD_DISMISSAL_RETRY_PLAN.md` at `768bf94f7b8832c17cbc120363b1928e4a1a024f`.
- Accepted source/failure lineage: `693e6dcbaa8525e05fea2b133dbed956d86d6537`, `72e8476899197f28f6ac428851d2613fe236a83d`, and `d9d8d1dedfdebe11ba2db06bc264bf55389a7362`.
- Control B `019fe020-e92e-7770-984f-b59acd547ab0` to Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Worktree/branch: `/private/tmp/shengfukung-wenfu-expo-v1-registration-runtime-keyboard-dismissal-retry`; `codex/expo-v1-registration-runtime-keyboard-dismissal-retry`.
- Packet/attempt: `2026-08-12-expo-v1-registration-runtime-keyboard-dismissal-retry-control-b`, attempt 16.

## Bounded keyboard-aware authority

- Only Pixel serial `39011FDJH00FQ8`, Pixel 8/`shiba`, and foreground `com.jimmy1768.komainu.dev/.MainActivity` are mutable. Fresh package-scoped hierarchy/screenshot and bounds are required before every event.
- Allowed ASCII values only: `Runtime Dependent`, `2`, `Runtime Contact`; no Director action, clipboard, alternate IME, inspector, system UI, source instrumentation or stale coordinate.
- After each text entry, prove keyboard visible, send exactly one Back, then prove keyboard absent, same form/package foreground, exact value retained and no navigation/save. Resolve post-IME button bounds freshly, outside former keyboard region, before one Create/Update.
- Stop, reset only if safely visible, then clean up on any unexpected screen/focus/value/IME/bounds event. No blind correction/retry or second Back.
- No source/config/test/dependency/native/Rails/Vue/plan edits except this report; no QR/camera/tenant/OAuth/provider/payment/admin/build/EAS/install/version/production/deploy/release/push/secret work.

## Execution and evidence

- Control `gpt-5.6-terra/high`; one report-only `gpt-5.6-terra/medium` Implementer. Medium is sufficient because the Implementer only performs static reporting, while Control handles bounded retained dummy state.
- Checks: exact ancestry/state; 51/51 tests/lint/verify/dependency equivalence; Pixel/package/foreground/listener/reverse; each text field’s complete IME lifecycle; one dependent; saved/reopened self and dependent drafts; paid guard/reset and cleanup; diff/status/staging.

## Sanitized runtime result matrix

| Evidence | Status | Safe result / limitation |
| --- | --- | --- |
| Entry/foreground/Metro and Runtime Dependent IME lifecycle | passed | Exact target, package, local dummy attachment, and entry state passed. The name field was empty; the permitted ASCII value rendered exactly while the IME was visibly present. One Back left the same dependent form and package foreground, with IME absent and value retained. Fresh post-IME bounds resolved the Create control outside the former keyboard area. One Create saved exactly one dependent; no extra character or duplicate was observed. |
| Self service create + quantity/contact IME lifecycles + saved reopen | untested | The selected current-temple admin-defined service preparation showed authoritative `TWD 600` and the required self choice, but its contact field was already nonempty. This contradicts the retry plan's required empty-contact precondition. Control stopped before creating or editing a draft; no quantity/contact IME lifecycle, Update, or reopen claim is made. |
| Dependent blessing draft saved/reopened | untested | Not reached after the unexpected self-service form precondition. No blessing draft, selection, or payment-related action occurred. |
| Paid guard, reset, cleanup | passed | No draft mutation occurred. The visible dummy reset restored the canonical dependent fixture and removed the packet-created dependent. Packet Metro, the exact serial reverse, temporary dependency symlink, and all packet UI evidence were removed; no listener, reverse, source residue, or staging remained. The installed client was preserved. |

## Terminal closeout

- Classification: `expo_v1_registration_authority_runtime_evidence_partial`.
- Continuation disposition: `no_evidence_backed_direct_repair_remaining`.
- First prevented criterion: Step 2 requires a self-service draft with contact field empty before authorized `Runtime Contact` entry. The actual prepared form retained a nonempty contact value. The immutable packet does not authorize clearing, changing, or normalizing that field before the specified precondition.
- Next owner/action: Planning must reconcile the accepted runtime plan's empty-contact assumption with the canonical dummy registration default and, if warranted, issue a new bounded evidence packet. This observation does not establish a source defect.
- Boundary confirmation: no source, configuration, test, dependency, native, Rails, Vue, payment, OAuth/provider, real API, build, deployment, production, release, push, or secret action occurred. No Director action was requested.

## Implementer static receipt

- Static packet review completed against `ops/docs/plans/EXPO_V1_REGISTRATION_RUNTIME_KEYBOARD_DISMISSAL_RETRY_PLAN.md` at `768bf94f7b8832c17cbc120363b1928e4a1a024f`; no runtime, Metro, ADB, device, test, or external action was performed.
- Confirmed source/failure ancestry: `693e6dcbaa8525e05fea2b133dbed956d86d6537`, `72e8476899197f28f6ac428851d2613fe236a83d`, and `d9d8d1dedfdebe11ba2db06bc264bf55389a7362` are all ancestors of the packet base.
- Confirmed each authorized text-field lifecycle requires exact pre-edit proof, a freshly resolved focused field, visible keyboard proof, permitted ASCII-only entry, exact rendered-value proof, exactly one target-fenced Back, and post-Back proof of absent keyboard, unchanged foreground package and same form, retained value, and no navigation or save.
- Confirmed each subsequent Create or Update requires fresh post-IME hierarchy/screenshot evidence and newly resolved visible bounds outside the absent/former keyboard region; no pre-dismissal coordinate is reusable. Any Back navigation, missing or ambiguous IME proof, changed value, unexpected focus, or ambiguous post-IME control requires an immediate stop without correction, retry, save, or second Back.
- Static scope result: this report is the only Implementer-owned path. Existing Control-packet sections are preserved. Git checks show this intended untracked report only; staging is empty. No staging, commit, merge, push, Planning message, or other mutation was performed.
