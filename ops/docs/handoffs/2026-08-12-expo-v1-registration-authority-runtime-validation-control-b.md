# Expo V1 registration authority runtime validation — Control packet

## Identity

- Accepted plan/base: `ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_VALIDATION_PLAN.md` at `37eb457cc8ea325beed967de1942499b760e9798`.
- Accepted alignment source: `693e6dcbaa8525e05fea2b133dbed956d86d6537`; canonical source baseline `92f00fd78869dfe1b2ee7d1207052bc861778a52` is an ancestor of the plan base.
- Control/Planning: Control B `019fe020-e92e-7770-984f-b59acd547ab0` -> Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Worktree/branch/base: `/private/tmp/shengfukung-wenfu-expo-v1-registration-authority-runtime-validation`; `codex/expo-v1-registration-authority-runtime-validation`; `37eb457cc8ea325beed967de1942499b760e9798`.
- Immutable packet/attempt: `2026-08-12-expo-v1-registration-authority-runtime-validation-control-b`, attempt 11.

## Exact runtime authority

- Only target: serial `39011FDJH00FQ8`, installed `com.jimmy1768.komainu.dev`, version `1.0.0`/code `1`/target SDK `36`.
- Only attachment: `adb -s 39011FDJH00FQ8 reverse tcp:8081 tcp:8081`; then `TEMPLEMATE_CLIENT_MODE=dummy BUILD_MODE=development npx expo start --dev-client --localhost --port 8081`; open only Metro's emitted local `exp+templemate` URL by target-fenced ADB.
- Packet-only visible input: `Runtime Dependent`, retaining the existing relationship; `Runtime Contact`; quantity `2` only for the self draft edit.
- Required evidence: all rows in the accepted evidence matrix, plus source/test/device/dependency entry fences and exact reset/Metro/reverse/symlink/evidence cleanup.

## Boundaries and lifecycle

- This is observation-only. Editable path is this immutable report only. No source/config/test/dependency/native/Rails/Vue/Plan edit; no real API/OAuth/provider/payment/admin/QR/camera/build/EAS/package/storage/deployment/release/push/secret action.
- One ephemeral Implementer: `gpt-5.6-terra/medium`, report preparation/static checks only. It cannot run Metro/ADB/device/symlink/external actions, stage, commit, merge, or push.
- Control owns all device actions, acceptance, cleanup, local report integration, and direct terminal delivery. If exact text input fails before compliant mutation, record `untested`; a reproducible visible failure is `defect`; do not repair.
- No intermediate Planning message except the accepted plan's exact callback (not expected in this QR/camera-excluded packet) or one terminal packet.

## Sanitized runtime evidence matrix

This matrix records Control-owned runtime observations. The Implementer itself
performed no Metro, ADB, device, symlink, or external action.

| Required evidence | Status | Sanitized observed result / limitation | Follow-up |
| --- | --- | --- | --- |
| Source ancestry, clean entry state, mobile test/lint/verify, dependency equivalence, and target/listener/reverse fences | passed | Plan/source ancestry; 51/51 mobile tests; lint; verify; byte-identical package/lock dependency evidence; exact Pixel/package/version/code/target-SDK; and clean listener/reverse fence passed. | Complete |
| Canonical dummy fixture state and signed-in account-only shell | passed | Signed-in dummy account shell reached without a render failure. | Complete |
| Exact dependent create using `Runtime Dependent`, with the prefilled relationship retained and no duplicate/concatenation | passed | One exact temporary dependent appeared with the retained relationship alongside the existing fixture. A preliminary malformed input was corrected before any committed create; no concatenated or duplicate row persisted. | Complete |
| Registrations permits new work only through Discover; no freeform offering entry | passed | New registration work was directed to Discover; no freeform offering entry appeared. | Complete |
| All three temple-defined offering titles and read-only TWD fees; no editable offering/money authority | passed | The deterministic catalog and read-only authority presentation were visible for all three offerings. | Complete |
| `線上祈福服務` preparation, self/dependent choices, and Cancel with no mutation | passed | Preparation showed permitted registrant choices; Cancel returned without a new registration or fixture mutation. | Complete |
| Self draft create at quantity 1, preserving the paid fixture and avoiding a duplicate | passed | One self draft was created; the paid fixture remained and no duplicate appeared. | Complete |
| Self draft metadata-only edit to quantity 2 and `Runtime Contact`, preserving offering/TWD 600 with no duplicate | untested | Before Update, target-fenced Android input/dev-inspector behavior exited the foreground. A later revisit showed the original quantity/contact, so no mutation was saved. | Future evidence authority if needed |
| Dependent draft for `平安祈福` using `Runtime Dependent`, preserving TWD 1,200 and the paid fixture | untested | The same input/foreground limitation occurred before a compliant dependent-draft mutation; no dependent draft was created. | Future evidence authority if needed |
| Paid fixture remains read-only with no payment, checkout, provider, offering, or fee edit surface | passed | The paid fixture remained display-only; no payment, checkout, provider, offering, or fee edit surface appeared. | Complete |
| Dummy reset restores the canonical dependent, paid/read-only registration, and offering catalog | passed | Reset restored the unbound canonical state with one fixture dependent, one paid/read-only registration, and the canonical catalog. | Complete |
| Packet Metro/reverse/symlink/evidence cleanup and clean Git/staging state | passed | Packet Metro process stopped; target reverse, temporary symlink, and evidence directory were absent; staging was empty and only this report remained untracked. | Complete |

## Static preparation receipt

- `693e6dcbaa8525e05fea2b133dbed956d86d6537` is an ancestor of the accepted
  runtime-plan base `37eb457cc8ea325beed967de1942499b760e9798`.
- Focused source inspection confirms the mobile registration screen directs
  new registration work to Discover and renders offering title/currency as
  display text; the runtime matrix remains the authority for visible behavior.
- `git diff --check` completed without output. No persistent source change was
  made; the temporary dependency symlink used for runtime dependency equivalence
  was removed during packet cleanup.

## Terminal closeout

- Terminal classification: `expo_v1_registration_authority_runtime_evidence_partial`.
- Continuation disposition: `no_evidence_backed_direct_repair_remaining`.
- Passed runtime evidence covers the entry fence, signed-in dummy shell, exact
  dependent creation, Discover-only registration entry, offering/fee authority,
  preparation cancellation, self draft creation, paid read-only behavior, reset,
  and packet cleanup.
- The self-draft update and dependent-draft creation rows remain `untested`,
  not defects: the target-fenced input/dev-inspector foreground limitation
  occurred before either compliant mutation, and subsequent observation showed
  no saved partial change.
- Next owner/action: Planning decides whether and how to authorize future
  evidence collection for the two remaining runtime rows. No source repair is
  implied by this report.
