# Control packet — OAuth account-resolution production read-only preflight retry

## Authority and immutable source

- Planning: Wenfu Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Accepted retry plan:
  `ops/docs/plans/OAUTH_ACCOUNT_RESOLUTION_PRODUCTION_READ_ONLY_PREFLIGHT_RETRY_PLAN.md`.
- Parent plan:
  `ops/docs/plans/OAUTH_ACCOUNT_RESOLUTION_PRODUCTION_READ_ONLY_PREFLIGHT_PLAN.md`.
- Exact retry base: `b3ff0288f33bf22622e22e994681b95278204671`.
- Isolated source: this worktree/branch
  `codex/oauth-account-resolution-production-preflight-retry` at
  `/private/tmp/shengfukung-wenfu-oauth-account-resolution-production-preflight-retry`.

## Local fence and live ownership

Before any production contact, prove this isolated worktree exactly equals the
retry base, is clean and unstaged; exact Candidate B tuple remains intact; and
canonical `main` is clean with the retry base as ancestor. Inspect any later
canonical commits by path. Continue only if every changed path is an already
authorized Expo Control report/packet or Planning callback/status documentation
and none touches Rails, deployment, OAuth, environment, dependencies, release
refs, production plans, or product source. Any other change stops without
production contact.

Control alone runs the parent plan's one exact target-fenced read-only
production sequence during one new 20-minute Asia/Taipei window. Retain only
the parent plan's sanitized fields. Any timeout, disconnect, stop condition,
output leak, or uncertainty ends the packet without retry, repair, or mutation.

## Persistent scope and Implementer

Persistent edits are limited to this packet and
`ops/docs/handoffs/2026-08-12-oauth-account-resolution-production-read-only-preflight-retry-control-a.md`.
One ephemeral `gpt-5.6-terra/medium` Implementer may perform local static
report/redaction review only. It has no production, SSH, network, database,
provider, secret, account, or runtime authority and must not stage, commit,
merge, push, or mutate external state.

## Closeout

Report corrected-fence proof, actual sanitized production fields or first remote
stop, pre/post equality/non-mutation proof, and smallest next Planning/Director
action. Local integration is limited to accepted packet/report records. No
release construction/ref movement, deploy/migrate/restart, flag/config/file/db
write, provider validation, account/session action, or historical remediation
is authorized.
