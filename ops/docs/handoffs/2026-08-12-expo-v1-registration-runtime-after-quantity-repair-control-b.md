# Expo V1 registration runtime after quantity repair — Control packet

## Immutable identity

- Plan/base: `ops/docs/plans/EXPO_V1_REGISTRATION_RUNTIME_AFTER_QUANTITY_REPAIR_PLAN.md` at `8f2c41e33a1c9ec2ea14acfb76a8cb093fe2c0bd`.
- Accepted quantity repair: `a26a48e66debce29c1146929f1d760ae839e595a`.
- Control B `019fe020-e92e-7770-984f-b59acd547ab0` to Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Worktree/branch: `/private/tmp/shengfukung-wenfu-expo-v1-registration-runtime-after-quantity-repair`; `codex/expo-v1-registration-runtime-after-quantity-repair`.
- Packet/attempt: `2026-08-12-expo-v1-registration-runtime-after-quantity-repair-control-b`, attempt 19.

## Bounded runtime authority

- Control `gpt-5.6-terra/high`; one report-only Implementer `gpt-5.6-terra/medium`. The Implementer performs static preparation only; Control owns all target-fenced dummy state, Metro, ADB, input, reset, cleanup, and evidence.
- Target only Pixel serial `39011FDJH00FQ8`, Pixel 8/`shiba`, package `com.jimmy1768.komainu.dev/.MainActivity`, version 1.0.0/code 1/target SDK 36. Use the exact local dummy USB Metro command and no Metro QR.
- Fresh package-scoped UI evidence/bounds precede every action. Allowed inserted values only: Runtime Dependent, 2, Runtime Contact. Apply exactly one Back after each insertion; exact `MOVE_END` and deletion counts only after proving known values.
- Validate exactly: dependent, self service default/create, repaired clear quantity + contact editing/update/reopen, dependent blessing/reopen, paid read-only, dummy reset and cleanup.
- No source/config/test/dependency/native/Rails/Vue/Planning change except this packet; no Director input, QR/camera/tenant/OAuth/provider/real API/payment/admin/build/EAS/install/version/production/deploy/release/push/secret work.

## Sanitized runtime result matrix

| Evidence | Status | Safe result / limitation |
| --- | --- | --- |
| Entry checks and Runtime Dependent creation | passed | Exact Pixel/package, dependency equivalence, dummy USB attachment, 52/52 mobile tests, lint, and verification passed. The canonical fixture state was used. One exact Runtime Dependent was created with the accepted visible-IME, one-Back, retained-value, fresh-Create-bounds sequence and no duplicate. |
| Self service create and repaired quantity/contact update/reopen | passed | The self preparation showed self, immutable TWD 600, quantity 1, and canonical contact 林小安. One Create saved a draft. On reopen, one Move End plus one Delete left quantity visibly empty, proving the repaired controlled input; `2` then survived the one-Back lifecycle. Contact 林小安 cleared exactly after Move End plus three Deletes; Runtime Contact then survived its one-Back lifecycle. Fresh Update bounds were tapped once. Reopen showed quantity 2 and Runtime Contact with unchanged offering/fee and no duplicate. |
| Dependent blessing selection/create/reopen | passed | Peace Blessing preparation showed immutable TWD 1,200 and the expected registrant choices. Runtime Dependent was selected once; one fresh Create saved exactly one dependent draft. Reopen showed the dependent and immutable offering/fee; the edited self draft and paid fixture remained. |
| Paid guard, reset, exact cleanup | passed | The paid fixture remained display-only with no payment/checkout action. Visible reset restored one fixture dependent and the one paid canonical registration, removing Runtime Dependent and both temporary drafts. Packet Metro, exact serial reverse, temporary dependency symlink, and packet UI evidence were removed; no listener, reverse, source residue, or staging remained. The installed client was preserved. |

## Terminal closeout

- Classification: `expo_v1_registration_authority_runtime_validation_complete`.
- Continuation disposition: `accepted_frozen_outcome`.
- First blocker: none.
- Boundary confirmation: no Director action/request, source/config/test/dependency/native/Rails/Vue/Planning repair, QR/camera/tenant, real API/OAuth/provider/payment/admin, build/EAS/install/version, production/deployment/release/push/secret, or other external action occurred.

## Static preparation receipt

- Observed 2026-08-12 in this isolated worktree before runtime: `HEAD` is the
  accepted plan base `8f2c41e33a1c9ec2ea14acfb76a8cb093fe2c0bd`, and accepted
  quantity-repair commit `a26a48e66debce29c1146929f1d760ae839e595a` is an
  ancestor.
- `mobile/app/account/registration_authority.js` exports
  `quantityInputValue(value)`, which returns `'1'` only for `null` or
  `undefined` and preserves an intentional empty value. `mobile/App.js` uses
  that helper only for the RegistrationForm quantity FormInput.
- Focused static coverage in
  `mobile/__tests__/registration-authority.test.js` records undefined, null,
  empty string, numeric/string `1`, numeric/string `2`, and `0` presentation.
- This Implementer performed no test, Metro, ADB, device, network, staging,
  commit, merge, or product-file action. Runtime evidence remains pending
  Control execution.
