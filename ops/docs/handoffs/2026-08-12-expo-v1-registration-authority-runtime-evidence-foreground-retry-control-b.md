# Expo V1 registration authority runtime foreground retry — Control packet

## Immutable identity

- Plan/base: `ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_RUNTIME_EVIDENCE_FOREGROUND_RETRY_PLAN.md` at `fa6273959f56af1a16457a15579e99c5d8aa0f6e`.
- Accepted source/predecessors: `693e6dcbaa8525e05fea2b133dbed956d86d6537`; `72e8476899197f28f6ac428851d2613fe236a83d`; `eb41760d72be0e7b366f80587bbb3b3987156b2f`.
- Control B `019fe020-e92e-7770-984f-b59acd547ab0` to Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Worktree/branch: `/private/tmp/shengfukung-wenfu-expo-v1-registration-authority-runtime-evidence-foreground-retry`; `codex/expo-v1-registration-authority-runtime-evidence-foreground-retry`.
- Packet/attempt: `2026-08-12-expo-v1-registration-authority-runtime-evidence-foreground-retry-control-b`, attempt 13.

## Exact authority

- Target only the unlocked Pixel `39011FDJH00FQ8` / `Pixel 8` / `shiba` with installed `com.jimmy1768.komainu.dev` `1.0.0`/code `1`/target SDK `36`.
- Before Metro/reverse, prove TempleMate is the visible foreground and NotificationShade is not focused. On failure, send exactly `director_action_required: templemate_foreground` and pause without any device workaround.
- Use only the approved dummy local Metro command, serial `tcp:8081` reverse, and local `exp+templemate` attachment after listener/readiness. No Metro QR.
- Recreate only minimum approved dummy setup. At each exact prepared form send the defined callback to Planning, then make no ADB field/selection action. The Director alone inputs self fields or selects the dependent; Control verifies and uses only one Update/Create after matching completion.
- No status-bar, unlock, system-navigation, package-launch, inspector, clipboard, keyboard/accessibility, storage/package, source, QR/camera, OAuth/provider/payment, build, release, production, deployment, secret, or push action.

## Execution and checks

- Control: `gpt-5.6-terra/high`. One ephemeral Implementer: `gpt-5.6-terra/medium`, lowest sufficient for report-only static preparation; it has no runtime/state authority.
- Implementer owns only this packet/report path; cannot run tests, Metro, ADB, device, symlink, callbacks, staging, commits, or external action.
- Control owns entry fence, tests, Metro/device actions, callback wait/resume, visible proof, reset, cleanup, review, commit, and direct terminal.
- Evidence: clean canonical/isolated state and ancestry; test 51/51/lint/verify; dependency equivalence; device/foreground/listener/reverse fence; successful bundle/normal foreground; exact before-save callback values; one save/create and reopened proof; paid guard/reset; exact cleanup and Git checks.

## Sanitized runtime evidence matrix

The rows below reflect completed Control observations. The Implementer prepared and static-checked this record only.

| Required evidence | Status | Safe result / limitation |
| --- | --- | --- |
| Foreground, entry, dependency, and exact Metro readiness | passed | Predecessor ancestry; 51/51 mobile tests, lint, and verify; dependency equivalence; exact Pixel/package/version/code/SDK; visible TempleMate MainActivity foreground without NotificationShade; approved listener/local attachment, bundle, and normal signed-in surface all passed. |
| Director-entered self values before Update | untested | Stopped before self-service draft setup and callback 1. |
| Saved/reopened self draft, immutable offering/fee, paid fixture | untested | Stopped before self-service draft setup or mutation. |
| Director-selected dependent before Create | untested | Stopped before dependent-registration form and callback 2. |
| Saved/reopened dependent draft, immutable offering/fee, paid fixture | untested | Required temporary dependent was absent; no dependent-registration form was opened. |
| Paid guard, reset, and packet cleanup | partial | Paid fixture was present before the stop; the final paid guard was not reached. Visible dummy reset restored the canonical unbound state. Metro stopped; serial reverse/listener absent; temporary symlink removed; no packet UI evidence was created; installed client untouched; report-only Git state/staging empty and diff check passed. |

## Implementer static receipt

- Verified in the isolated worktree at `fa6273959f56af1a16457a15579e99c5d8aa0f6e` that accepted predecessors `693e6dcbaa8525e05fea2b133dbed956d86d6537`, `72e8476899197f28f6ac428851d2613fe236a83d`, and `eb41760d72be0e7b366f80587bbb3b3987156b2f` are ancestors of the packet base.
- Confirmed callback 1 is emitted only from the exact self service edit form after visible offering `線上祈福服務`, read-only `TWD 600`, self, quantity `1`, and contact/Update controls; after Planning's exact completion relay, Control verifies quantity `2` and `Runtime Contact`, performs one Update, and proves saved/reopened immutability, no duplicate, and retained paid fixture.
- Confirmed callback 2 is emitted only from the exact dependent form after visible `平安祈福`, read-only `TWD 1,200`, quantity `1`, all required choices, and no existing Runtime Dependent/offering draft; after Planning's exact completion relay, Control verifies Runtime Dependent selected and self unselected, performs one Create, and proves saved/reopened immutability, no duplicate, and retained paid fixture.
- No runtime, device, Metro, ADB, callback, test, symlink, staging, or commit action was performed by the Implementer.

## Terminal closeout

- Classification: `expo_v1_registration_authority_runtime_evidence_partial`.
- Disposition: `true_planning_design_gap`.
- Entry/foreground/Metro evidence passed, and the visible signed-in baseline contained exactly the canonical dependent and paid fixture; `Runtime Dependent` was absent.
- The accepted plan requires creation of `Runtime Dependent` in deterministic setup but supplies no permitted interaction method: its two Director callbacks begin only after that setup, while ADB text, clipboard, inspector, keyboard/accessibility, and invented callbacks are prohibited. Control did not create the dependent or mutate a registration form.
- This is a planning-method gap, not a product or source defect. Planning must authorize the exact creation method (for example, one bounded Director callback) before another retry.
- No private fixture data or raw local attachment URL is recorded in this report.
