# Wenfu Control Implementation Packet

## Identity

- Accepted plan and immutable criteria: `ops/docs/plans/TEMPLEMATE_PHASE_3_HEADER_UTILITY_AND_SINGLE_LINE_NAVIGATION_PLAN.md` at `98ee9180ba488870281f96e5f035e89ff0c110a0`; accepted source baseline `e0dcd0b078e10510c3d87c4981d664912e008c02`.
- Control authority: Wenfu Control B / `019fe020-e92e-7770-984f-b59acd547ab0`; target Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Repository/worktree/branch/base: `/Users/jimmy1768/Projects/shengfukung-wenfu`; `/private/tmp/shengfukung-wenfu-templemate-phase3-header-single-line-nav`; `codex/templemate-phase3-header-single-line-nav`; `98ee9180ba488870281f96e5f035e89ff0c110a0`.
- Immutable packet identity and implementation attempt: `2026-08-16-templemate-phase3-header-utility-single-line-navigation-control-b`, attempt 10.

## Scope

- Objective: make Settings a bound Header utility immediately beside Sign out; retain sign-out-only unbound Header; make the five business destinations the exact non-wrapping horizontal main navigation with readable labels and touch targets.
- Owned editable paths: `mobile/App.js`, `mobile/app/account/screen_model.js` only if its pure business-menu authority needs alignment, `mobile/__tests__/account-surface.test.js`, `mobile/__tests__/ui-refinement.test.js`, at most one existing focused presentation test, and this Control record.
- Excluded: copy, adapters/repositories, tenant/OAuth/camera/QR/registration/payment behavior, Rails/Vue, dependencies/lockfile/config/native/version/build, Metro/ADB/device, provider/production/deployment/release/push/external action.
- Required checks: focused menu/header/layout assertions; full mobile test, lint, verify; identity/config boundary scan; exact path/diff review; clean final isolated and canonical states.

## Repair And Terminal Boundary

- Bounded repair within unchanged criteria: no at packet creation.
- True Planning design gap if the requested header utility or single-line menu requires an excluded navigation framework, copy/model semantic change, or widened surface.
- Planning receives no intermediate status; terminal only after independent Control acceptance or a true unchanged-criteria stop.

## Implementer Dispatch

- Persistent Handoff: no. This is bounded presentation/test work; no handoff eligibility.
- Selected ephemeral Implementer: `gpt-5.6-terra` / medium, the lowest sufficient allocation for a contained Expo JavaScript layout and regression-test patch.
- One Implementer task: edit only owned paths; no staging, commit, merge, runtime/device, dependency, external action, or scope expansion; return directly to this Control.

## Control Review And Closeout

- Independent source review: the candidate changes only `mobile/App.js`, `mobile/app/account/screen_model.js`, `mobile/__tests__/account-surface.test.js`, and `mobile/__tests__/ui-refinement.test.js`. It uses the existing `navigate('settings')` feedback path; keeps the unbound `TenantSetupGate` Header sign-out-only; makes `accountMenu()` the exact five business destinations; and replaces wrapping business tabs with a horizontal non-wrapping ScrollView without destination-label truncation.
- Focused checks: `node --test __tests__/account-surface.test.js __tests__/ui-refinement.test.js` passed 10/10; `yarn lint`, `yarn verify`, and `git diff --check` passed. Exact-path/config boundary review passed.
- Required full suite: `yarn test` reached 57 passing tests but failed loading `__tests__/expo-oauth-runtime.test.js` because this fresh source-only worktree lacks `mobile/node_modules/expo-crypto/build/Crypto.types.js`. The failure precedes the test body and is an environment materialization gap, not evidence of a candidate source defect.
- Authority boundary: the accepted source-only plan excludes dependency materialization, install, symlink, or network action. Control did not create a dependency tree, retry the suite with a substitute, modify source to evade the test, stage/commit the candidate, or integrate it.

## Terminal Disposition

- Classification: `source_candidate_unaccepted_required_full_check_unavailable`.
- Disposition: `director_decision_or_authority`.
- First prevented criterion: passing the mandatory full mobile suite in a fresh worktree.
- Next owner/action: Planning/Director must separately authorize a bounded, exact dependency-materialization/check continuation if it wants this preserved candidate considered for acceptance. The source candidate remains unaccepted and unintegrated; canonical main is unchanged.
