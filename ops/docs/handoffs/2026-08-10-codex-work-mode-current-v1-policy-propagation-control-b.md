# Control B Implementation Packet — Current Codex Work Mode V1 Policy Propagation

## Identity

- Accepted-plan path and immutable criteria: `/Users/jimmy1768/Projects/shengfukung-wenfu/ops/docs/plans/CODEX_WORK_MODE_CURRENT_V1_POLICY_PROPAGATION_PLAN.md`.
- Control task and authority state: Wenfu Control B; ordinary reversible local builder-governance propagation accepted by Planning under registered Strategy gate `codex_work_mode.package.f737dc3.v1`.
- Repository, worktree, branch, and base HEAD: `/Users/jimmy1768/Projects/shengfukung-wenfu`; same; `main`; `48b6d00fe38681fa1ca150f9f65e58a3d06f778f`.
- Packet status and date: immutable implementation packet, 2026-08-10.

## Scope

- Objective: Install exact OperatorKit `f737dc3ef92f54f31685e5526bece10cb9bb80fc` package bytes and propagate the current v1 local routing, repair, receipt, protected-validation, immutable-artifact, and rationale-bound allocation policy only across the frozen governance surface.
- Exact owned editable paths:
  - `.agents/skills/codex-work-mode/SKILL.md`
  - `.agents/skills/codex-work-mode/codex_work_mode_skill_package_manifest.yml`
  - `ops/docs/reference/codex_work_mode.md`
  - `ops/protocol/codex_work_mode.yml`
  - `ops/docs/handoffs/templates/codex_control_implementation.md`
  - `ops/docs/handoffs/codex_work_mode_current.md`
  - `ops/scripts/verify_codex_work_mode_skill.sh`
  - this packet, only for Control closeout after acceptance.
- Required behavior:
  - Copy only committed OperatorKit package bytes for `SKILL.md` and the self-excluding manifest. Resulting package hashes must be `6547d2aeee198bcc9e16c6f8fb6120f0efd443fe1d39b828ecaff2ca872182ae` and `0afe96c5f10ad621c720d248e3dcd4d84e98279de62052933b5d250cd8ca623a`; existing `agents/openai.yaml` remains byte-identical at `f79867a4869ff4cc5c6638955346f43d3e84f1c54c7a1d42fa9f1416f50ad007`. The package has exactly three files and no other OperatorKit content.
  - Update only owned local sources to express Planning-to-Control local routing; Strategy-only cross-repository/lifecycle/registered-gate changed-evidence traffic; the exact local misroute response; Control-owned nonterminal bounded repair and final terminal disposition; one terminal packet plus paired Planning receipt; immutable-artifact language; protected-validator requirements and uncertain-outcome fence; and the immutable current allocation.
  - Update deterministic verification to prove all frozen requirements, including no stale First-Tenant-plan pointer, and preservation of `AGENTS.md` and `agents/openai.yaml` from the Planning base.
- Explicitly excluded paths and systems: `AGENTS.md`; `.agents/skills/codex-work-mode/agents/openai.yaml`; all other `.agents` paths and any OperatorKit content; Planning plan; product/runtime/config/env/schema/deployment/provider/secret/data files; external systems, push, deployment, production work.
- Required checks and expected evidence:
  - direct `git -C /Users/jimmy1768/Projects/operator-kit show f737dc3ef92f54f31685e5526bece10cb9bb80fc:<package path>` byte comparisons;
  - all required SHA-256 values, exact three-file package closure, manifest payload closure, and valid YAML;
  - `bash -n ops/scripts/verify_codex_work_mode_skill.sh` and focused verifier pass;
  - deterministic routing/repair/protected-validation/model/residue scans, unchanged protected paths against base, `git diff --check`, clean staging/status after integration.
- Evidence sources and status:
  - Documented: the committed Planning plan supplies exact OperatorKit commit, identity, hashes, owned surface, exclusions, and registered gate.
  - Observed: current package and verifier retain superseded package hashes/routing/allocation and a stale First-Tenant verifier pointer; these are the direct in-scope correction surfaces.
  - Configured: `AGENTS.md` source map and package descriptor establish `AGENTS.md`/`agents/openai.yaml` as preservation surfaces.
- First blocked surface, if known: none.

## Incident-Correction Placement

- Is this an incident correction? No; this is a Strategy policy propagation already accepted as a local governance implementation phase.
- Selected surface: portable package payload, direct local governance references/protocol/template/snapshot, and deterministic verifier only.
- `AGENTS.md` excluded unless explicit Director authorization is recorded: excluded.

## Handoff Eligibility (Before Model Selection)

- Persistent Handoff requested: no.
- Eligibility confirmed before selecting a model: yes; one ephemeral Implementer is required.
- Luna disqualifiers checked: availability, cost, mechanical simplicity, and rejection do not qualify.

## Implementer Dispatch

- Selected model and reasoning: `gpt-5.6-terra/high`.
- Selection reason and lowest-sufficient configuration: exact committed-byte replacement must be reconciled with several tightly coupled protocol/reference/template/verifier semantics, protected-validator constraints, and exhaustive deterministic residue checks. This is explicit bounded governance complexity, justifying Terra/high.
- Ephemeral allocation: justified `gpt-5.6-terra/high`; Luna is never ephemeral.
- One ephemeral Implementer task: implement only the scope above and return evidence.
- Return destination: this Control directly.
- Implementer boundaries: owned paths only; no acceptance, staging, commit, merge, push, deploy, approval handling, secret access, external mutation, or scope expansion.

## Control Review And Closeout

- Conformance review against immutable criteria: verify exact package bytes/closure, protected-path preservation, deterministic local policy semantics, and absence of runtime/external scope.
- Acceptance decision and rationale: accepted. Direct committed-byte comparisons for every package file match OperatorKit `f737dc3`; the package closure is exactly three files, protected `AGENTS.md` and descriptor are byte-preserved, and the local governance sources/verifier deterministically encode all immutable routing, repair, receipt, protected-validation, terminology, and allocation criteria. No product/runtime scope is present.
- Integration, staging, and commit evidence when accepted: Control independently ran direct three-file comparison, all required SHA-256/closure checks, protected-path comparison from Planning base, YAML parsing, `bash -n ops/scripts/verify_codex_work_mode_skill.sh`, the focused verifier, `git diff --check`, and staged-diff-empty checks. Local commit pending this closeout record.
- Terminal packet to Planning: send exactly one immutable terminal packet after local integration; wait for Planning paired receipt.
- Residual risk, production gap, and next owner: no product/runtime impact; Planning alone sends compact registered-gate completion to Strategy after accepting the local terminal packet.
- Authority confirmation: Planning froze local immutable criteria under the registered Strategy gate; Control sends no local status/receipt to Strategy.
