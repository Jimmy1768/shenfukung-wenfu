# Expo V1 registration authority alignment — Control packet

## Identity

- Accepted plan and immutable criteria: `ops/docs/plans/EXPO_V1_REGISTRATION_AUTHORITY_ALIGNMENT_PLAN.md` at `65c69f12a448fe433430bb61fab7bf3a2a600b31`.
- Control/Planning: Control B `019fe020-e92e-7770-984f-b59acd547ab0` -> Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Repository/worktree/branch/base: `/Users/jimmy1768/Projects/shengfukung-wenfu`; `/private/tmp/shengfukung-wenfu-expo-v1-registration-authority-alignment`; `codex/expo-v1-registration-authority-alignment`; `65c69f12a448fe433430bb61fab7bf3a2a600b31`.
- Immutable packet and attempt: `2026-08-12-expo-v1-registration-authority-alignment-control-b`, attempt 1.

## Bounded scope

- Objective: replace TempleMate's freeform registration draft with the existing current-temple, Temple-admin-defined offering authority from discovery through preparation, create, and permitted metadata edit.
- Editable paths: only the plan-owned native account registration/resource controllers, existing native serializers or one small native offering presenter, focused native registration/resource tests, `mobile/App.js`, necessary focused account state below `mobile/app/account/`, existing `mobile/app/dummy/`, `mobile/app/real/`, `mobile/app/ui/copy.js`, focused mobile tests, and this packet.
- Explicit exclusions: models/schema/migrations/admin/browser/Vue/OAuth/provider/payment/checkout/Expo config/dependency/lockfile/native project/version/build/deployment/release/push/external action and all runtime/device/Metro paths.
- First blocked surface: none. If implementation requires a schema, model, admin, browser semantic, or unowned authority change, return `true_planning_design_gap` without broadening this packet.

## Evidence and checks

- Rails: focused native registration/resource contract tests and browser account registration regressions; Ruby syntax for every changed Ruby path; route/response and forbidden-field scans.
- Mobile: focused dummy, real-adapter, registration-state, discovery, and UI tests; full `yarn test`, `yarn lint`, `yarn verify`; no fallback, authority, identity/money-field, payment/admin/OAuth residue scans.
- Repository: `git diff --check`, staged diff check, exact owned-path review, clean isolated/canonical status with empty staging.
- Dependency policy: use only an already present byte-identical temporary `node_modules` symlink if tests require it; remove it before review. No install/copy/materialization.

## Allocation and boundaries

- Persistent Handoff: ineligible. This is a single bounded local source/test packet.
- One ephemeral Implementer: `gpt-5.6-terra/high` because the accepted contract spans Rails tenant-scoped authority, immutable money/identity boundaries, native request shape, and coordinated dummy/real state without a schema change.
- Implementer boundaries: packet-owned paths only; no staging, commit, merge, push, deployment, provider/secret/external access, device/runtime action, plan edit, or scope expansion. Return evidence directly to Control.

## Control review and terminal boundary

- Control independently reviews plan conformance, required checks, tenant and authority boundaries, changed paths, and integration readiness.
- Planning receives only one terminal disposition: `accepted_frozen_outcome`, `true_planning_design_gap`, or `no_evidence_backed_direct_repair_remaining`; no intermediate status packets.
- If accepted, Control commits the bounded source result and integrates it locally onto canonical `main`; no runtime validation follows without a new Planning packet.
