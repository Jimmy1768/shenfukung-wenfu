# Wenfu Control Navigation Height Runtime Confirmation Packet

## Identity

- Accepted plan: `ops/docs/plans/TEMPLEMATE_PHASE_3_NAVIGATION_HEIGHT_RUNTIME_CONFIRMATION_PLAN.md` at `fa484015cb545fef4986d0c7c877fc16ce147ddf`; accepted repaired source `90ebd1461fe16644c54e85b73c29719585b89f93`.
- Control: Wenfu Control B / `019fe020-e92e-7770-984f-b59acd547ab0`; target Planning `019fea6a-c481-75d1-b9d8-6aea367ca5b6`.
- Worktree/branch/base: `/private/tmp/shengfukung-wenfu-templemate-phase3-navigation-height-runtime-confirmation`, `codex/templemate-phase3-navigation-height-runtime-confirmation`, `fa484015cb545fef4986d0c7c877fc16ce147ddf`.
- Immutable packet identity: `2026-08-16-templemate-phase3-navigation-height-runtime-confirmation-control-b`; attempt 16.

## Scope

- Objective: use exactly one ordered package/DevLauncher/Metro attachment to present the accepted compact navigation repair for Director confirmation, without source repair or app-state mutation.
- Allowed local/device mutation: one frozen-lockfile project-local materialization and node_modules cleanup; temporary stay-awake `2` with prior-value restoration; one exact-package force-stop; one URL-free start; one Metro listener; one exact reverse; one local URL delivery; packet-local sanitized visual evidence.
- Required checks: source/Git/manifests/package/port/reverse/awake-unlocked prior-stay-awake fences; focused/full/lint/verify; visible DevLauncher readiness; bounded stable UI; retained bound shell matrix if available; exact cleanup.
- Excluded: source/test/config/dependency/lock/version/native change; app-data/cache clear; package/install/reboot; QR/camera; real API/OAuth/provider/payment; Rails/Vue; production/deployment/release/push/secrets/external action.

## Dispatch

- One ephemeral Implementer: `gpt-5.6-terra` / medium, lowest sufficient for static preflight only. It may not materialize dependencies, run tests, access Metro/ADB/device, edit source or the packet, stage, commit, merge, push, or use external services.
- Control owns all materialization, runtime, UI observation, Director-ready callback, cleanup, acceptance, and terminal delivery.

## Director Callback And Terminal Boundary

- Only after a stable bound shell satisfies the full compact-height matrix will Control send exactly `director_action_required: navigation_height_visual_confirmation` to Planning and hold the explicit session.
- Signed-out or unbound state is reported truthfully and is not mutated to manufacture the menu. A context-not-ready soft log is nonfatal only if running main and stable UI follow.
- No Planning traffic occurs before the exact callback or one terminal outcome.

## Control Runtime Evidence

- Entry fences passed: the isolated worktree was at the exact accepted base with the repaired source as an ancestor; canonical main was clean; the target was the exact Pixel 8/shiba serial and the installed TempleMate development client reported version `1.0.0`, version code `1`, and target SDK `36`.
- The required manifest and lockfile hashes were unchanged before and after the one allowed frozen-lockfile materialization. The locked Expo Crypto runtime file was present. The packet-created `node_modules` tree was removed during closeout.
- Checks passed: focused account/navigation tests `10/10`; full mobile suite `58/58`; lint; and verify.
- The recorded prior USB stay-awake value was `0`; Control set the packet-scoped USB-only value `2` once and verified it before runtime.
- The ordered lifecycle completed once only: exact-package force-stop without clearing data; URL-free exact MainActivity start; visible DevLauncher readiness; dummy localhost Metro readiness; exact serial `tcp:8081` reverse; and one established local development-client URL delivery. Metro reported a completed Android bundle and the package remained foreground on MainActivity.
- The resulting visible app surface was stable but signed out. It was neither a retained bound shell nor the authenticated-unbound gate. The plan forbids sign-in or tenant mutation merely to reach the navigation menu, so the compact-height navigation, centered-pills, Header Settings + Sign out, and adjacent-content visual matrix are untested rather than inferred.
- Minimal package-scoped log review found a context-not-ready soft exception followed by `Running "main"`; it did not prevent the stable visible app surface. No fatal exception, Render Error, or native process exit was observed in the bounded evidence.

## Cleanup And Terminal

- Control stopped only the packet Metro process, removed only the exact serial reverse, restored `stay_on_while_plugged_in` to the recorded prior value `0`, and removed the packet-created dependency tree and temporary screenshots. Final listener and reverse checks were empty; the installed client and its data were preserved.
- `git diff --check` passed. Canonical main remains unchanged and clean at the accepted plan base. The isolated branch contains only this immutable packet/report before its terminal commit.
- Terminal classification: `navigation_height_runtime_confirmation_signed_out_state`.
- Disposition: `no_evidence_backed_direct_repair_remaining`.
- First prevented criterion: the retained bound shell required for Director visual confirmation was unavailable, and the frozen plan prohibits manufacturing it through sign-in or tenant-state mutation.
- Next owner/action: Planning must decide whether a separately bounded, explicitly authorized dummy account/session preparation packet is warranted for the visual navigation matrix. No source repair is indicated by this packet.
